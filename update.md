# 助教優化說明：`training-iter-v11` → `feature/product-ui`

> 對照範圍：`training-iter-v11`（2026-07-24）→ `feature/product-ui`（2026-08-07）
> 共 49 個 commit。上一份對照（`hybrid-review-backstop` → `training-iter-v11`）
> 保留在 `training-iter-v11` 分支的 `update.md`。

## 一句話總結

`training-iter-v11` 的助教是「**功能都在，但要會用命令列，而且教到後半段常常走鐘**」；
這一版是「**貼上題目就能用的圖形介面，而且教到後半段真的收得住手**」。

**模型完全沒換**（仍是 `qlora_adapter_v9`）。這一輪的所有改善都來自
**驅動程式的確定性修復、備課管線的擴充，以及守門本身變得更會抓錯**。

---

## 對照總表

| 助教面對的情境 | training-iter-v11 | 現在 |
|---|---|---|
| 一般使用者想用它 | 只能敲命令列、自己維護 session 檔 | **瀏覽器介面**：貼題目 → 看備課進度 → 直接對話 |
| 學生自己帶來的題目卡住了、需要更深的提示 | 沒有提示梯可用，等級 2 退化成一句通用指示，還提早一輪被推進逐步教學 | 備課時**自動生成兩條分級提示**，與內建題走同一條升級路徑 |
| 學生一直卡住、給到最深的提示還是不會 | 支援強度在「深提示 ↔ 淺提示」之間**來回震盪**，同一段話重複三輪 | 支援強度**單調遞增**，第 4 次卡住就進逐步教學 |
| 學生交出草稿、助教指出缺漏、學生追問 | 交稿當下就被判定「已完成」，追問被路由進收尾 → **糾錯中斷** | 交稿不算完成，**審閱通過**才收尾；追問正常走糾錯 |
| 證明其實已走完，但全程沒有「交稿」這一步 | 助教下總評式肯定後，driver 補一句「下一步該從哪裡下手？」 | 認出這個情境，改請他**交稿**，接回審閱→收尾 |
| 助教親口說「整個證明完成了」 | 同一則回覆裡還會被補上一句追問（時序缺口） | 本輪生成後**立刻**進入收尾，不再自打嘴巴 |
| 學生的證明有缺漏，但助教想稱讚他 | 可能說「完全正確、無懈可擊」＝**替錯誤背書** | **稱讚校準守衛**：只肯定真正對的部分、點出還沒處理好的地方 |
| 同學模式下助教講得興起 | 大量「完全正確／你已完全掌握」——沒有解法卻替學生掛保證 | 同儕版背書守衛，任何「整份論證」等級的總評都會被重寫 |
| 重新生成後的回覆 | **直接送給學生**，不再檢查洩漏／奉送／算式 | 每一次重生成都**重跑一遍全部內容防護** |
| 學生說「我不會」但寫得很長 | 沒有 `even` / `really` 這類副詞插入時才認得（英文） | 容許副詞插斷；同時**推出新式子時不算卡住**（少浪費提示） |
| 逐輪命令列的收尾修復 | 狀態每輪歸零 ＝ 修復**等於沒上線** | 整包狀態快照，新增欄位自動跟著存 |

---

## 一、圖形介面：從「開發者工具」變成可以給人用的東西

`dataset/app.py`（Gradio 本機單人 Demo）。使用者**自帶題目**，完全不查題庫。

```
貼上題目（＋可選的證明嘗試）
   └─ 逐階段串流顯示備課進度：PROVER 1/3 → VERIFIER → REPAIR → SEGMENTER → LADDER
        ├─ ✅ 備課完成（grounded） → 全部防護生效的引導教學
        └─ ⚠️ 沒能驗證出可靠解     → 同學模式（誠實降級）
   └─ 對話區逐輪引導；「重新開始」換一題
```

工程上刻意處理的三件事：

- **備課不會卡死 UI**：背景執行緒 + 佇列串流，worker 以 `try/finally` 保證送出結束哨兵，
  備課拋例外時畫面會顯示錯誤並留在輸入態。
- **不會被連點塞爆**：生成期間同時停用「送出」鈕與輸入框（只停用按鈕的話按 Enter 照樣送出），
  加上 `default_concurrency_limit=1`——這是單張 6GB 卡的 Demo。
- **三層可測結構**：純邏輯（`check_ollama` / `assemble_problem` / `opener_for`）獨立於
  Gradio，`test_app.py` 無 GPU、不連 Ollama 即可跑。

Code review 兩輪判定：**可作為本機單人 Demo 上線，無 High/Medium 缺陷**（`docs/code-review-product-ui.md`）。

啟動方式與畫面說明見 `README.md`「圖形介面」節與 `使用者說明書.md`。

---

## 二、學生自己帶來的題目，現在也有分級提示梯

這是本輪功能面最大的一塊，也是上一版一個**沒被發現的空洞**。

備課管線原本只產「參考解」與「教學步驟」，**不產提示梯**。內建的 17 題有人手寫的
`hint_ladders.json`，使用者自帶的題目則沒有——於是等級 2 只剩一句通用指示
（提示深度改由 4B 模型當場即興拿捏，正是設計上最想避免的），而且因為「梯長算 1」，
**提早一輪就掉進逐步教學**：該被第二條提示救起來的學生直接被講答案。

| 連續卡住 | 有梯（內建題） | 無梯（舊行為） |
|---|---|---|
| 1 | 等級 1 | 等級 1 |
| 2 | 等級 2，給第 1 條提示 | 等級 2，**通用保底句** |
| 3 | 等級 2，給第 2 條提示 | **直接進逐步教學** |
| 4 | 進逐步教學 | — |

修法是在備課管線加第五階段 **LADDER**，規格由 33 條手寫提示反推：恰兩條、依序對應
證明的兩個關鍵轉折、每條點名一個定理／構造／性質的名稱與作用、**不得出現任何算式**。

生成後過**五道確定性驗收**：恰 2 條非空字串／每條 12–60 字／不帶新算式／不洩漏參考解／
兩條彼此不重複。**任一不過就整份丟棄**，行為退回今日的樣子——所有失敗路徑
（Ollama 離線、無法解析、驗收不過）收斂到同一結果，**最壞情況零退步**。

端對端實測 2 題皆產出通過驗收的提示，且確實依序對應該題真正的兩個轉折：

- 「數列收斂則有界」→ ①收斂定義＋三角不等式取尾端的界 ②前綴有限集取最大值
- 「連續函數在閉區間有最大值」→ ①波爾查諾-魏爾斯特拉斯定理推有界 ②序列準則使上確界被達到

> 「不得帶新算式」那道特別重要：等級 2 的禁算式白名單**包含當前提示文字**，
> 提示若帶算式會讓該守衛對那些算式失效。

---

## 三、學生一直卡住時，支援強度不再倒退

上一版有一個很傷的行為：學生連續卡關時，助教的支援等級序列是 `0→1→2→1→2→1`——
給過一次深提示後**掉回淺提示**（指示是「拆更小的子問題、仍不點名定理」＝比上一輪給得更少），
逐步教學也被反覆重置拖延。實際對話裡表現為同一段話逐字重複三輪：

> 也許你該先查查這個序列的圖形長什麼樣子，再試著猜它的逐點極限。那你覺得，下一步該從哪裡下手？

學生連說「毫無頭緒」，助教卻叫他「自己去查圖形」。

診斷過程本身值得記：**前兩批都在修症狀，第三批才找到根因。**

1. 第 1 批：driver 自己補的保底句把重複偵測的相似度稀釋到門檻以下（實測 0.768 < 0.85）
   → 比對前先剝除 driver 補的句尾。
2. 第 2 批：重新生成回來的仍是同一段話，程式卻**無條件採用** → 重生成後複驗，
   教學輪推進到下一個步驟重講。
   → 逐字重複確實消失了，**但教學品質評分還是最低分**，失敗模式只是換出口
   （改成直接洩漏答案後宣告放棄）。
3. 第 3 批（根因）：`_tutor_turn` 在送出等級 2 提示後把「連續卡住次數」歸零。對**恢復的**
   學生那是對的（但那種情況本來就會歸零），對**持續卡住**的學生則是有害的——刪掉那一行即可。

結果：等級序列變成 `0→1→2→2→逐步教學`，實測對話成為單調遞增的降階梯
（抽象提問→簡化→具體操作→點名策略→給關鍵點→寫出代入計算），無重複、無放棄。

**副作用也一併處理了**：逐步教學每輪最多重生成 4 次曾讓學生每輪等待變成 3–4 倍，
現在加了每輪重生成次數上限，並跳過教學輪那次「勸他別重複」的無效重生成。

---

## 四、卡住偵測變準了（量測驅動，不是憑感覺）

標註 251 則真實學生訊息做量表後才發現：**原本的判定誤判很多**——學生語氣遲疑
（「我不太確定…」）但其實推出了新式子時，也會被算成卡住、白白消耗一條提示。

加上「訊息帶進新數學內容就否決卡住判定」後，精確度 **0.484 → 0.737**（誤判 16 → 5）。

另修一個英文漏接：原本要求 `don't know` / `not sure` **連續**出現，
口語極常見的 "I don't **even** know" / "I'm not **really** sure" 被副詞插斷而漏掉——
學生明說不會卻拿不到提示升級。改為容許中間插最多兩個詞，並補上「不可誤殺肯定句」
（`I know exactly…` / `I'm sure…`）的測試。

---

## 五、收尾紀律：四條路徑全部補上

上一版已經有收尾狀態，但**只接得住有明確訊號的兩條路徑**。一次真實對話稽核加上
守門的 6 場對話，逐場追出四個獨立缺口：

| 缺口 | 症狀 | 修法 |
|---|---|---|
| **交稿當下就 arm** | 學生交稿 → 助教指出缺漏 → 學生追問 → 被路由進收尾（助教被指示「已完成、不要再問」）＝**糾錯就此中斷** | 交稿當輪不 arm；改由**審閱通過**訊號決定（審閱輪的回覆不含問句＝沒再要求修正） |
| **靠措辭比對宣告完成** | 實測 8 種自然措辭只命中 2 種（「邏輯無縫，你已經完全掌握」「證明已經完成」皆漏接），漏接就補上一句「那你覺得，下一步該從哪裡下手？」 | 主訊號改為上述確定性訊號；措辭正則保留給「對話中途自然證完」，並補三道閘 |
| **助教自己請學生交稿沒被記下** | 助教常自己開口請學生寫證明，driver 沒記下來 → 151 字草稿走不到交稿分支 → 不進審閱 → 拿不到通過訊號 | 掃助教上一則回覆補記；措辭須帶「完整／整份／整個」整份範圍，避免「寫出第一步」被誤判 |
| **時序缺口** | 偵測放在下一輪開頭、保底句是本輪結尾補的 → 「宣告完成」與「下一步該從哪裡下手」出現在**同一則回覆** | 抽出共用函式，本輪生成後立即進入收尾，補句前就生效 |

另外補上「全程沒有交稿步驟」的情境（證明在一問一答中走完）：三道閘同時成立時
（對話 ≥6 則、學生上一則 ≥80 字、助教下了總評式肯定）改補一句**請他交稿**，
接回既有的審閱→收尾路徑；閘沒過就維持通用追問，避免推導途中的單步肯定被誤判成整份完成。

真實對話重放驗證：三場對話末輪的防護紀錄**全部清空**，通用追問由 2 次降為 0 次。

---

## 六、稱讚校準：不再替錯誤背書

新增守衛，兩種情況命中即重寫：

- **錯誤背書**：審閱後盾已回報缺漏，回覆卻不含問句地說「完全正確／無懈可擊」
- **誇飾腔**：訓練集出現 0 次的措辭（邏輯無縫／嚴謹性之魂／超過大多數）＝基底模型漂移

局部肯定＋用問句點出缺漏不會被誤殺——重寫指示要的是「只肯定真正正確的那一部分，
明確點出還沒處理好的地方」。

**同學模式另有一道**：原本這道守衛被關在「非同學模式」的分支裡，同儕模式完全不生效，
正是實測中「首輪誠實聲明有效、之後大量『完全正確／你已完全掌握』」的根因。
沒有可靠解法卻替整份論證掛保證，是同學模式最不該有的口吻。

---

## 七、守衛鏈本身的五項缺陷

對驅動程式做 code review，逐項以可執行證據確認後修復（全程先寫會失敗的測試）：

1. **重生成繞過內容防護（最嚴重）**：回問保底與重複偵測的重生成結果**直接落地**，
   不再經洩漏／防奉送／禁算式／稱讚校準——全檔唯一未經內容檢查就送到學生面前的路徑。
   實測可讓一段逐字複製參考解、結尾帶問號的回覆完整落地，正好架空招牌的抗洩漏防護。
   → 四道防護抽成共用函式，**每次重生成後再跑一次**。
2. **順序缺陷**：重複偵測原本排在回問保底之後，它的重生成會把剛補上的保底問句整個蓋掉
   → 保底保證失效。改為排在前面。
3. **交稿偵測 mid-proof 誤命中**：「你能自己試著寫出這個證明的第一步嗎」也會被記成交稿請求；
   而這是個單向閂，誤設後真正該交稿時再也進不了交稿階段。→ 措辭須帶整份範圍。
4. **拒絕判定過寬**：回覆任何位置出現「不能／不會／無法」就整條否決，實測 4 句合理的
   交稿請求誤殺 2 句。→ 改為只看命中片段所在的子句。
5. **特殊階段消耗提示梯**：階段有值時 system 注入的是階段指示、根本沒有提示內容，
   卻照樣把提示記為已用；更糟的是逐步教學的進入條件就是「提示梯用盡」，實測**一串糾錯輪
   就能把梯吃光，學生一條提示都沒拿到就被推進逐步教學**。→ 只在一般引導輪推進。

---

## 八、守門本身變得更會抓錯（本輪最有價值的一塊）

功能改得再多，沒有量測就不知道有沒有真的改善。這一輪對守門動的刀比對助教還多：

### 補上兩個結構性盲區

- **真實對話回放（Tier 0，無 GPU）**：`test_phase_routing.py` 把守門累積的
  **299 場真實對話**（49 輪存檔、1974 輪）回放進確定性層，檢查 5 條不變式。
  動機：手寫的單元測試每句台詞都是人寫的——能證明狀態機邏輯正確，**證明不了真模型
  講出來的話會不會踩中那些正則**（收尾／交稿／完成宣告偵測全在比對散文）。
  靈敏度已驗證：把修復還原回去，不變式確實會響，證據逐字重現當年的 bug。
- **多輪確定性探針（Tier 1b）**：原本 Tier 1/2 全是單輪探針，測不到多輪行為——
  連續 6 輪守門、期間改了守衛鏈順序與卡住判定語意，104 筆回覆**始終逐字相同**。
  新探針一跑完就抓到還沒修的問題（見下方「已知未解」）。

### 量出守門自己的雜訊

`measure_gate_noise.py`：取 14 輪同後端、回覆**逐字全同**的守門（生成端零變化），
差異即評審自身的不確定性。結果讓人清醒——**9 個硬性評審指標中有 8 個的純雜訊
≥ 自己的容忍值**，亦即沒有任何真實退步時也可能判退。

而且**放寬容忍不是解法**：某指標共 14 題，真實「多錯 2 題」的幅度比雜訊還小，
任何蓋得住雜訊的容忍值都會同時放行真實退步。處置是把最不可靠的幾個指標降為
advisory（照算照印、不進 pass/fail），並在多次評審上取共識。

> 由此得到本輪最重要的紀律：**判退時的第一個動作是逐字比對本輪與上輪的回覆存檔**。
> 全同 ⇒ 生成端沒變 ⇒ 所有評審分數波動都是雜訊。這一輪的多次守門都是這個情況——
> 分數上下浮動，但那不是本批修復的功勞，也不是它的過錯。

### 一個關於測試的教訓

真實對話回放的第 4 條不變式**初版是空的**：它用一個「排除帶問句回覆」的函式做判斷，
而該函式本身永遠不可能觸發。**每條不變式都必須實際還原一次 bug 驗證會響，否則只是裝飾。**

---

## 驗證方式

決策邏輯全部是確定性程式碼，可離線驗證，不需要 GPU：

```powershell
python dataset\test_driver_unit.py      # 313 條斷言 / 32 組，數秒完成
python dataset\test_phase_routing.py    # 299 場真實對話回放，5 條不變式
python dataset\test_app.py              # 介面純邏輯（不連 Ollama）
```

備課管線可單獨試跑（需 Ollama）：

```powershell
python dataset\auto_reference.py --statement "證明 ..." --id NEW1 --out new_problem.json
```

完整守門（GPU＋評審 CLI，約 1.5–2 小時）最後一輪 **exit 0，25 項硬性指標全 ≥ 基準**
（計分卡 `dataset/regression_scores/2026-08-06T194135_5426de5.json`）。

---

## 已知未解（下輪優先）

1. **英文多輪對話會洩漏參考解（4 題中 3 題）**——Tier 1b 多輪探針首度跑完即抓到，
   尚未修。值得注意的是單輪的抗洩漏指標長期滿分，可見**「單輪不洩漏」完全推不出
   「多輪不洩漏」**；這正是新探針補上的那塊盲區，補上後第一次量就見底。
2. **自動生成的提示梯只有安全性驗收、沒有品質驗收**。五道閘擋的是「洩漏／給算式／
   退化重複／格式錯」，擋不掉「講得爛但無害」；而且守門走的全是內建題（有手寫梯），
   **這條路徑在守門指標上是隱形的**，品質只能人工抽查。
3. **英文 session 會拿到中文提示內容**：備課只生繁中梯，英文的回退順序是
   「英文梯 → 中文梯」。已知取捨，不擴充雙語備課。
4. **對話中途的錯誤步驟仍然沒有檢查**：審閱後盾只在審閱／糾錯輪觸發，學生在推導途中
   寫錯一步（又沒說「這樣對嗎」）時，模型手上沒有任何「這步錯了」的訊號。
5. **助教自己的數學錯誤沒有任何攔截點**：現有防護沒有一道檢查內容正確性。
6. **逐步教學仍偏弱**：重複迴圈修好後評分回升，但新的評語指向另一個問題——
   助教在子目標之間跳躍，沒把一個走完。
