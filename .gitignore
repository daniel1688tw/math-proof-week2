# 大型模型權重與量化檔（不進 repo，本機/HF Hub 管理）
learn_path/socratic_tutor/qwen3_4b/
gguf/
*.gguf

# LoRA adapter 權重（>100MB，超過 GitHub 單檔硬限制；程式碼與資料集已足夠重現訓練）
# 用萬用字元涵蓋所有版本（v2、v3、v4、v5…），避免每次新版本都要手動補規則。
qlora_adapter/
qlora_adapter_v*/
*.safetensors

# build.py 的中間產物：core/aug 兩份分流檔的內容 100% 併入 train.jsonl + val.jsonl，
# 全 repo 沒有任何程式讀取它們（只有 build.py 寫），重跑 build.py 即可重生。
dataset/dialogues_core.jsonl
dataset/dialogues_augmented.jsonl

# Python
__pycache__/
*.pyc

# Claude Code 本機設定（不含機密，但屬個人環境設定，不應進 repo）
# 例外：.claude/skills/ 是專案工作流程（如 /pre-push-check 守門），要進版控
.claude/*
!.claude/skills/

# 訓練/評估 log 與打包產物（zip 可用 git archive 重新產生）
*.log
*.zip
qlora8b_adapter_v*/

# 伺服器訓練的本地暫存（HF cache 等大檔不進 repo）
server_train/workspace/out/
server_train/workspace/hf_cache/

# 機密：伺服器/主機清單與連線指令（含內網位址、帳號），禁止進 repo
COMMANDS.md

# 本機探索用 notebook（個人暫存，不進 repo）
test.ipynb
train.ipynb
