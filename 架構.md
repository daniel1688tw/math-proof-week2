# simple_4B_ollama.py 架構設計

> 對照組「直接送 LLM」管線的 **Qwen3-4B-Thinking + 本機 Ollama** 版本。
> 與 `simple.py`（Qwen2.5-Math-7B / transformers）功能對應，但換模型、換推理後端，並針對「思考型模型」的特性做了兩項關鍵設計（採樣參數、截斷續寫）。

---

## 1. 為什麼要有這支（設計動機）

| 問題 | 在此檔的對策 |
| --- | --- |
| 這台筆電網路擋掉 HuggingFace 檔案 CDN，抓不動 transformers 的 safetensors 權重 | 改用 **Ollama**（走自己的 registry / 本機已有 GGUF），不經 HF |
| `transformers 5.x + bitsandbytes` 在 Windows 載入模型會 segfault | Ollama 底層是 **llama.cpp**，徹底繞開 transformers/bnb |
| Qwen2.5-Math-7B 是「答案調優」模型、不自我檢查，證明常有漏洞 | 換成 **Qwen3-4B-Thinking-2507**（RL 訓練的思考型模型，先 `<think>` 再作答） |
| 思考型模型在最難題會把生成預算耗在思考鏈、答案被截斷成空 | **截斷偵測 + 自動續寫**（見 §4） |

核心定位：**「最簡架構」**——不做 Extract/Verify/Correct 多階段（那是 `baseline.py` 的事），只負責「把題目送進去、把乾淨的最終證明取出來」。複雜度都在「如何正確地與思考型模型互動」。

---

## 2. 整體資料流

```
raw_problem (str)
   │
   ▼
run_simple_4b_ollama()
   │  1. _resolve_model()         ← 讀 ollama_model.txt 決定模型 tag
   │  2. 組 messages（system + user）
   │  3. 組 options（採樣 + num_ctx/num_predict）
   │  4. POST /api/chat  (think=True)   ─────────►  本機 Ollama (llama.cpp)
   │  5. 解析 message.thinking / message.content
   │  6. 若 done_reason=="length" → 續寫一次（見 §4）
   ▼
{"reasoning": ..., "answer": ..., "model": ...}   或   answer (str)
```

外層由兩支測試驅動呼叫：
- `run_smoke_4b_ollama.py`：跑 1 題（冒煙健檢）
- `run_eval_4b_ollama.py`：跑全 10 題（完整評估，每題增量存檔）

---

## 3. 關鍵設定（Configuration 區塊）

| 參數 | 值 | 設計理由 |
| --- | --- | --- |
| `DEFAULT_MODEL` | `qwen3:4b` | 後援值；實際以 `ollama_model.txt` 為準（目前為 `qwen3-4b-thinking-2507:latest`） |
| `TEMPERATURE / TOP_P / TOP_K` | `0.6 / 0.95 / 20` | **Qwen 官方對思考模型的建議值**。切勿用 greedy / 低溫——思考模型在低溫下會退化（重複、繞圈） |
| `NUM_PREDICT` | `15360` | 生成上限（思考＋答案）。最難題思考鏈可達 ~2 萬字元，需要大預算 |
| `NUM_CTX` | `16384` | context window。**實測 20480 會讓 KV cache＋權重達 6.0GB → 30% offload 到 CPU、速度大降**；16384 全程留在 6 GiB GPU 內。若仍截斷，靠 §4 補完而非加大 ctx |
| `REQUEST_TIMEOUT` | `1800s` | 4050 上的長思考單題可達數分鐘，需放寬 HTTP timeout |
| `SYSTEM_PROMPT` | — | 要求「嚴謹、完整、逐步、用精確定義與定理名」，並**禁止 'obvious'/'clearly' 等含糊用語** |

---

## 4. 核心機制：截斷偵測 + 自動續寫

這是本檔相對於 `simple.py` 最重要的新增設計，直接對應思考型模型的失效模式。

**問題**：難題的思考鏈太長，把整個 `num_predict` 預算耗光，模型還沒輸出 `</think>`／最終答案就被迫停止 → `message.content`（最終答案）為**空**，或寫到一半被**截斷**。在全 10 題驗證中，#3 / #10 因此得 0 分、#6 半截。

**判斷依據**：Ollama 回傳的 `done_reason`。正常收尾是 `"stop"`；被長度截斷是 `"length"`——後者幾乎必然代表輸出不完整（無論答案是空還是半截）。

**對策**（不靠無限加大 num_ctx，省 VRAM）：
```
if done_reason == "length":
    1. 把已生成的 thinking + content 接回成 assistant 訊息
    2. 追加 user 指令：「你已思考足夠，停止思考，只輸出完整最終證明」
    3. 第二輪請求關閉 think（think=False）→ 整個預算留給答案
    4. 用第二輪的 content 取代原本的空／半截答案
```
半截的答案會被完整版**取代**而非附加，避免重複片段。

---

## 5. 函式分工

| 函式 | 職責 |
| --- | --- |
| `_resolve_model()` | 讀 `ollama_model.txt` 取得模型 tag；讀不到則用 `DEFAULT_MODEL` |
| `_split_think(text)` | **後援**切割器：舊版 Ollama 不回傳結構化 `thinking` 欄位時，從 `</think>` 把推理與答案切開 |
| `_post_chat(payload)` | 對 `/api/chat` 發 POST（stdlib `urllib`，無第三方依賴），回傳解析後的 dict |
| `run_simple_4b_ollama(raw_problem, model, return_thinking)` | **主入口**：組訊息、呼叫、解析、截斷續寫。`return_thinking=True` 回 dict（含 reasoning），否則只回 answer 字串 |
| `_print_answer(...)` | CLI 顯示（思考鏈只報字數、隱藏內容，只印最終證明） |

---

## 6. 推理/答案分離策略（兩層保險）

1. **優先**：用 Ollama 結構化欄位——`message.thinking`（推理）與 `message.content`（答案）天然分開（請求帶 `think=True`）。
2. **後援**：若 `thinking` 為空（舊版 Ollama 把思考內嵌在 content），用 `_split_think()` 從 `</think>` 切割。
3. **相容**：若模型/版本不接受 `think` 參數而回 HTTP 400，自動移除 `think` 重送一次。

---

## 7. CLI 介面

```powershell
# 互動模式（逐題輸入）
python simple_4B_ollama.py

# 單題模式
python simple_4B_ollama.py "Prove that ..."
```
（`-f xxx.json` 形式的參數會被忽略、退回互動模式，避免誤把檔名當題目。）

**前置條件**：Ollama 服務在跑（`http://localhost:11434`）、目標模型已註冊（`ollama_model.txt` 指向的 tag 存在）。

---

## 8. 設計取捨小結

- **只用 stdlib（urllib/json）**：零第三方依賴，不受 transformers/bnb 環境問題影響，最大化可攜性。
- **VRAM 優先於 context 長度**：6 GiB 對「4B-q4＋長 context」本就吃緊，寧可把 `num_ctx` 壓在 16384（全 GPU）＋靠續寫補完，也不加大 ctx 換取 CPU offload 的大幅降速。
- **單次呼叫為主、續寫為例外**：不做多階段 pipeline；只有偵測到截斷才多送一輪，把工程複雜度降到最低，同時解掉思考型模型唯一的主要失分源。

---

## 附註：檔案落點（中文路徑提醒）

本專案路徑含中文「霓的資料」，在 Windows 上實測存在 **NFC/NFD 兩種正規化的同名目錄**，可能造成「檔案工具」與「PowerShell 手動 `Set-Location`」落到不同實體目錄。本檔與相關產物（`simple_4B_ollama.py`、`compare_simple.csv`、`improve_v2.md` 等）以**檔案工具的工作目錄**為準。若用 PowerShell 操作找不到檔案，多半是進到了另一棵同名目錄。
