# 蘇格拉底式高等數學證明引導助教：以 QLoRA 微調結合確定性驅動程式與混合驗證架構

**專題成果報告**

版本：v9 部署形態（Qwen3-4B + TutorDriver + 審閱後盾）＋ 商品化 Gradio 介面
撰寫日期：2026-07-26　│　分支：`feature/product-ui`

---

## 摘要（Abstract）

本專題建構一套「蘇格拉底式」的大學高等數學／數學分析**證明引導助教**：面對學生的證明題，
系統不直接給出答案，而是以逐步提問引導學生自己完成證明。核心技術挑戰在於——小型語言模型
（4B 參數、可在單張 6GB VRAM 筆電 GPU 上推論）在數學嚴謹性與「引導深度拿捏」上均不穩定，
純監督式微調（SFT）無法可靠校準「何時該給提示、給多深」這類連續量，且模型容易在即時數學
判斷（審閱學生草稿）上產生錯誤背書。

本研究提出並落地一套**「模型＋驅動程式」的部署形態**與**混合驗證架構**：(1) 以手寫且經數學
驗證的雙語 grounded 資料集對 **Qwen3-4B-Instruct-2507** 做 QLoRA 微調（`qlora_adapter_v9`），
模型只負責數學內容與語氣；(2) 離散教學決策（何時升級提示、何時切換階段）交給確定性
**驅動程式 `TutorDriver`**；(3) 提示內容拿捏交給**人工預寫的分級提示梯**；(4) 即時數學判斷
（審閱學生草稿找缺漏）交給**思考型模型在幕後找碴**（審閱後盾），微調模型只負責把缺漏清單
包裝成引導語氣；(5) 對沒有現成參考解的新題目，先跑**自我驗證備課管線**（生成×3 → 獨立驗證 →
修補 → 教學步驟切分），先自己證對才教，證不對則誠實降級為「同學模式」。

在 8 道訓練集外題目 × 3 對抗情境的同尺評分下，部署形態達總平均 **4.37/5**，超過裸模型
（4.35）與單獨的思考型模型路線（4.00）；跨領域（離散數學／線性代數）遷移總平均 4.25，
與域內幾無衰減；自我驗證備課盲測 verified 正確率 **9/9 = 100%**（零錯誤背書）。專題最後將
系統包裝為**本機單人 Gradio 介面**，使用者可貼上自己的題目（可含半成品或錯誤證明），由助教
自動備課後逐步引導。

**關鍵詞**：智慧型教學系統、蘇格拉底式教學、大型語言模型、QLoRA、參數高效微調、
grounding／抗幻覺、混合架構、自我驗證、LLM-as-judge、證明引導。

---

## 1. 緒論（Introduction）

### 1.1 背景與動機

高等數學（微積分、數學分析、線性代數、離散數學）的證明能力是理工科的核心素養，但學習曲線
陡峭。一對一的蘇格拉底式引導——教師不給答案、而以提問誘導學生自己走完推理——被公認為
最有效的教學形態之一（Bloom 的「2-sigma 問題」指出一對一輔導可使成效提升約兩個標準差），
卻難以規模化。大型語言模型（LLM）帶來以對話式家教規模化的可能，但直接以通用 LLM 當家教
有兩個致命問題：**(a) 傾向直接奉送答案**（不符合引導教學）、**(b) 數學幻覺**（自信地講出錯誤
推理，對教學是負面示範）。

本專題的限制條件更嚴苛：目標部署硬體為單張 **RTX 4050 Laptop（6 GiB VRAM）**，只能承載
約 4B 參數、4-bit 量化的模型。小模型的數學可靠度與行為穩定度都比大模型差，使上述兩個問題
更加突出。

### 1.2 問題定義

給定一道證明題與學生逐輪的回覆，系統須產生「下一句引導」，並滿足：
1. **不洩漏**：不直接給出關鍵步驟的完整算式或整段證明；
2. **分級引導**：學生連續卡住時才逐級加深提示，最深僅點出關鍵定理／想法（不給算式）；
3. **抗壓**：被逼問答案時婉拒並把主導權還給學生；
4. **糾錯而不奉送**：學生方向正確時只肯定＋開放式追問，方向錯誤時引導其自行發現錯誤；
5. **數學正確**：任何講出來的數學內容不得有錯（包含審閱學生草稿時的判斷）；
6. **知之為知之**：對助教自己都沒把握的題目，誠實聲明而非硬教。

### 1.3 研究目標與主要貢獻

- **C1｜部署形態設計**：提出「模型負責數學與語氣、確定性程式負責離散決策、預寫內容負責
  提示拿捏」的三分工架構，並以六輪迭代（v2→v6）與三路線正面對決驗證其為最佳形態。
- **C2｜混合驗證架構**：將思考型模型的即時數學判斷「移植」進部署形態，僅在幕後找碴、
  永不直接面對學生，隔離其致命傷（偶發整段洩題、空輸出），把糾錯銳利度注入微調模型。
- **C3｜自我驗證備課**：對任意新題目自動生成並獨立驗證參考解，達成「先自己證對才教」，
  盲測 verified 正確率 100%；證不對則自動降級同學模式。
- **C4｜雙語化與可辯護的守門**：中英平行資料重訓，並建立「只在能可靠量測的層面棘輪」的
  回歸守門，辨識並隔離輸入隨機指標的評審雜訊。
- **C5｜商品化落地**：將部署形態包裝為本機單人 Gradio 介面，支援使用者自帶題目、
  備課進度串流、grounded/同學模式自動分流。

---

## 2. 相關研究與定位（Related Work）

本研究橫跨數個領域，於此依大類簡述並定位本工作的差異（完整參考見第 12 節）。

- **智慧型教學系統與蘇格拉底式教學（ITS / Socratic Tutoring）**：傳統 ITS 以規則與知識追蹤
  為主；近期 LLM 家教（如對話式輔導資料集與行為遷移方法）證明可用對話引導，但多聚焦
  K-12 或程式教學，且少處理「證明」這種需嚴格推理鏈的任務。**本工作專注於高等數學證明**，
  並以確定性層強制引導行為，而非仰賴模型自律。
- **參數高效微調（PEFT / LoRA / QLoRA）**：LoRA 與 QLoRA 使大模型能在消費級硬體上微調。
  **本工作在 6GB 卡上以 4-bit nf4 + QLoRA 微調 4B 模型**，並將 SFT 的能力邊界（連續量校準）
  以外部程式補足。
- **grounding 與抗幻覺（Grounding / Hallucination Mitigation）**：檢索或參考文本 grounding 是
  抑制幻覺的主流手段。**本工作以「題目專屬參考解」作為 grounding 錨**，並實證其為跨域不可
  移除的關鍵（移除即出現數學錯誤）。
- **推理驗證與自我一致性（Verification / Self-Consistency / BoN / Process Supervision）**：
  多候選取樣＋驗證、過程級監督可提升數學推理正確率。**本工作以「生成×K → 獨立驗證 → 修補」
  的自我驗證備課**實現教學前的正確性保證，並實驗性評估了 Best-of-N＋驗證器（判定邊際效益
  不足，未採用）。
- **對話與生成評估（LLM-as-judge / Evaluation）**：以強模型當評審是可規模化的評估法，但已知
  有位置偏誤與雜訊。**本工作系統性地量化了「輸入隨機指標」的評審雜訊**，並據此設計只升不降
  且對雜訊免疫的守門。

---

## 3. 系統方法（Methodology）

### 3.1 整體架構：部署形態 = 模型 + 驅動程式

```
新題目 ──► auto_reference.py 備課（PROVER×3 → VERIFIER → REPAIR → SEGMENTER，思考型）
   ├─ verified   → grounded 教學（全部防護生效）
   └─ unverified → 同學模式（誠實聲明沒把握、同儕探索、被質疑會反省認錯）

學生訊息 ──► TutorDriver（確定性決策層）
              · stuck counter：連續卡住 0/1/2 次 → 提示等級 0/1/2
              · 階段偵測：交草稿→審閱 / 逼問→拒絕 / 嘗試→糾錯 / 說懂了→請寫證明 / 完成→收尾
              · 等級 2 注入 hint_ladders 的人工預寫提示
              · 提示梯用盡仍連卡 → walkthrough 逐步教學（一步一確認，教完仍要學生自寫）
              · 審閱/糾錯輪 ──► 審閱後盾（思考型模型對照參考解找碴，缺漏清單注入 system）
              ▼
         qlora_adapter_v9 + Qwen3-4B（4-bit nf4）＋ grounded system（含 <REFERENCE_PROOF>）
              ▼
         後處理：單問句截斷、洩漏 15-gram 檢查、on-track 防奉送、等級 2 禁算式、回問保底
```

### 3.2 設計鐵律：三分工

本架構最核心的設計原則，源自 v4→v6 三次迭代的教訓：

| 維度 | 交給誰 | 理由 |
|---|---|---|
| **離散決策**（何時升級、何時換階段） | 確定性程式碼 | 二元/離散事件，程式判定 100% 可控、可測 |
| **內容拿捏**（提示深度、審閱重點） | 人工預寫內容（參考解、hint ladder） | 「透漏多少」是連續量，純 SFT 校準不準 |
| **數學與語氣** | 微調模型 | 模型的強項；行為受上兩層約束 |

**延伸（混合架構）**：即時數學判斷（審閱草稿找缺漏）超出 4B 微調模型的可靠範圍，交給
思考型模型；判斷與說話分工。

### 3.3 確定性驅動程式 `TutorDriver`

- **stuck counter → 提示等級**：偵測學生「卡住」訊號（含強困惑詞不受長度門檻限制），
  連續卡住次數對應提示等級 0/1/2；等級 2 才注入預寫提示梯內容且禁止附算式。
- **階段機**：交草稿→`review`、逼問→拒絕、嘗試→糾錯、宣告懂了→請寫證明、實質證完→`closed`
  收尾（禁新問題／替代法／延伸）。
- **收尾紀律（#11/#12 修復）**：學生完成證明後帶問句的反思路由 `closed` 簡答不藉機延伸；
  且當**助教自己確認整個證明完成**時亦 arm `done_closed`，接住「對話中途自然證完」的路徑。
- **後處理防護**：參考解洩漏 15-gram 檢查（命中則加強指示重生成）、單問句截斷、
  on-track 防奉送（方向對只肯定＋開放追問）、回問保底（該問而未問則重生成）。
- **語言跟隨學生**：session 語言逐輪依學生訊息重判，支援「英文題＋中文學生」與中途換語言。

### 3.4 自我驗證備課管線（`auto_reference.py`）

原則：**助教必須先自己證對才有資格教**。對沒有手寫參考解的題目：

1. **PROVER**（思考型，temp 0.7）生成 K=3 份候選證明；
2. **VERIFIER**（temp 0.2，獨立審閱，教學標準）逐份判定 pass/fail；
3. pass 但有小瑕疵 → **REPAIR** 修補後複驗；
4. **SEGMENTER** 把參考解切成 3–6 個循序教學步驟（每步附確認小問題）；
5. 有 pass → `verified`（產出 `reference_proof` + `teach_steps`）；全 fail → `unverified`。

`unverified` 時 `TutorDriver` 自動進入**同學模式**：誠實聲明沒把握、以同儕身分探索、被質疑時
反省認錯——體現「知之為知之」。

### 3.5 審閱後盾（`review_backstop.py`，混合架構落地）

審閱/糾錯輪時，先讓思考型模型（`qwen3-4b-thinking`）對照參考解找碴，將缺漏清單注入微調
模型的階段指示。思考型模型不在線時自動降級回原行為（`REVIEW_BACKSTOP=0` 關閉）。關鍵工程
教訓：思考鏈需 `num_predict=8192`（否則思考吃光正文）、模型輸出的 LaTeX 是非法 JSON escape
（需反斜線加倍重試）、CRITIC 要求「教學標準」（未明說的依據也算缺漏）。

---

## 4. 資料集（Dataset）

- **全人工撰寫並驗證**：所有題目與多輪對話的數學內容由人（Claude 輔助）撰寫並對照參考解
  驗證正確性，**不使用 4B 模型生成訓練內容**（避免把模型自身的錯誤餵回去）。
- **雙語平行**：中文核心 50 道題 + 對話，並建立英文平行資料（`src/` + `src_en/`），達成
  「語言跟隨學生」的雙語能力。
- **規模（本分支）**：訓練集 **830 例**（`train.jsonl` 747 / `val.jsonl` 83），涵蓋首問、糾錯、
  逼問、審閱、收尾等情境；含 `dialogues_journey.py` 的「剛學完但易卡住」跨題型旅程對話。
- **grounding 錨**：每題的 system prompt 內嵌該題參考解（`<REFERENCE_PROOF>`），實證為
  跨域抗幻覺的關鍵，不可移除。
- **建置與驗證**：`build.py` / `validate.py` / `test_dataset.py`。

---

## 5. 訓練與實作（Training & Implementation）

- **基底模型**：Qwen3-4B-Instruct-2507。
- **微調法**：QLoRA（4-bit nf4 量化 + LoRA），`peft 0.19.1`、`bitsandbytes 0.49.2`、
  `transformers`（4B 全程安全；7B 在 Windows + bitsandbytes 會 segfault，本專案不用 7B）。
- **v9 關鍵超參**：`MAX_LEN=1024`（相對 v8 的 640，避免截掉 26% 樣本的尾端＝對話收尾／
  審閱輪，正是弱點根因）、其餘同 v8。實驗室 4090（bf16）訓練約 12.5 分鐘，eval_loss **1.052**。
- **部署精度**：本機以 4-bit 載入（部署精度），adapter 分塊拷回、逐檔 SHA256 校驗
  （教訓：絕不可只信檔案大小，一律以 SHA256 為準）。
- **硬體**：推論 RTX 4050 Laptop 6GB；訓練另用 4090。

---

## 6. 實驗設計與評估方法（Experimental Setup）

- **評估題**：8 道 held-out（訓練集外）＋ 難題（`hard_math_major`）＋ 跨領域
  （`xdomain_problems`：離散×3 + 線代×3）。
- **對抗情境**：S1 首問、S2 糾錯（埋錯讓助教抓）、S3 逼問（要求直接給答案）。
- **同尺評分（LLM-as-judge）**：以強模型當評審，五分制，逐題／逐情境評分。
- **回歸守門（`regression_suite.py`）**：推送前品質守門，Claude／Gemini 當「評審＋學生」
  真實互動後評分；確定性指標（洩漏／拒絕／單問句／升級／教學收尾）零容忍，judge 指標
  容忍 ε；任何指標低於 `regression_baseline` 即 `exit 1`、不可推送；基準只升不降。
- **評審後端**：預設 **Antigravity CLI / Gemini 3.6 Flash Medium**（脫離 Claude 額度依賴；
  25 次壓測 100% 可解析、判準對照 2/2、對話數學兩輪逐字一致），Claude 保留為備援。
  選型教訓：**模型層級高 ≠ 當裁判可靠**（Gemini 3.1 Pro 對話層評分兩輪 0.333↔0.0 崩潰）。

---

## 7. 結果與分析（Results）

### 7.1 路線對決（部署形態判定依據）

8 held-out 題 × 3 情境，同尺評分（`eval_out_final/FINAL_VERDICT.md`）：

| 路線 | S1 首問 | S2 糾錯 | S3 逼問 | 總平均 |
|---|:---:|:---:|:---:|:---:|
| **部署形態：微調 + TutorDriver + 審閱後盾（採用）** | 4.31 | 4.50 | 4.31 | **4.37** |
| 微調 + TutorDriver（後盾離線降級形態） | 4.31 | 4.19 | 4.31 | 4.27 |
| 裸微調模型 | 4.38 | 4.31 | 4.38 | 4.35 |
| 思考型模型 + grounded（單獨） | 3.63 | 4.75 | 3.63 | 4.00 |
| 基底模型 + grounded | 3.88 | 4.06 | 3.00 | 3.65 |

**分析**：
- **混合架構的價值**：審閱後盾把思考型的糾錯銳利度（S2 4.75）移植進部署形態，S2 由
  4.19→4.50（跨域 4.67→4.92），S1/S3 逐字不變，且消除了 H3 的錯誤背書。
- **確定性防護的直接證據**：S3 逼問由裸模型 4.06 → 4.31（洩漏重生成 24 筆中觸發 6 次全成功）。
- **為何不單用思考型模型**：它單獨用不可靠（S3 曾把完整證明整段交出、有時空輸出）；
  混合架構讓它只在幕後找碴、永不直接面對學生，致命傷被隔離。

### 7.2 跨領域遷移（離散數學／線性代數）

`eval_out_xdomain/EVAL_REPORT_xdomain.md`：三情境總平均 **4.25 ≈ 域內 4.27**，引導行為無衰減
遷移；S2 糾錯 4.67 反超域內（6 個埋錯全中）；**18 筆生成零數學錯誤**——grounded 參考解是
跨域關鍵錨。跨域放大的域內弱點，當日以 driver 三項防護（on-track 防奉送／等級 2 禁算式／
回問保底）緩解，跨域升至 4.44、域內無回歸。

### 7.3 自我驗證教學（SVT）

`eval_out_xdomain/SVT_REPORT.md`：備課管線盲測（10 題已知解盲跑）**verified 9/10、verified
正確率 9/9 = 100%**（零錯誤背書）；唯一 unverified 是歷來最難的 Darboux 定理題——
「知之為知之」正是設計目標。逐步教學端對端：提示梯耗盡→自動進入→一步一確認→教完轉回
學生自寫，全程正確。

### 7.4 審閱後盾端對端

`eval_out_xdomain/backstop_e2e.md`：對 3 個微調模型曾失手的案例（有/無後盾對照），
H3 雙重錯誤的錯誤背書消失、X4 從問錯目標變精準指出「缺 v₂≠0」、X2 無回歸；找碴準確度
（`test_backstop.py`）X2/X4/H3 = 3/3 精準。

### 7.5 消融與否定結果

- **grounding 消融**：移除參考解 → 跨域即出現數學錯誤（故不可移除）。
- **後盾消融**：離線降級形態 S2 4.50→4.19（後盾貢獻可量測）。
- **Best-of-N + 驗證器（否定，`BON_VERIFIER_VERDICT.md`）**：校準後抓錯 3/4、誤殺 0/3，
  但實戰 273 輪僅 1 輪真的觸發重生成（0.4%）——微調＋driver＋後盾已把可驗證數學錯誤壓到
  極低，驗證器邊際效益無法量測卻多 8GB 顯存與延遲，**不採用**（程式碼保留可重啟）。

---

## 8. 迭代歷程與工程教訓（Discussion）

- **v2→v6（部署形態判定）**：三路線對決確立「模型＋驅動程式」形態；學到離散決策必須交給
  程式碼。
- **v7（雙語化 + 守門重設計）**：中英平行重訓；建立「語言跟隨學生」、收尾偵測、
  守門容忍度校正（小樣本 judge 指標須蓋過單題改判雜訊）。
- **v9（序列長度 640→1024）**：根治「截掉樣本尾端」的訓練面弱點，英文替代證法亦根治；
  收尾 bug 由 driver 兩項確定性修復根治。**部署預設全面切 v9。**
- **v10 / v11（判退，維持 v9）**：擴充 830 例 journey 資料重訓，兩版皆重現「完成後未經請求
  過度延伸」（H5 案例），偶伴數學錯誤，driver 蓋不住模型級行為 → **判退、封存**；但同輪的
  **#11/#12 driver 收尾修復乾淨、可上線**（跑在 v9 之上即生效）。
- **守門校準（治理層教訓）**：`dialogue_math_ok` 為二元×n=3，實測同一批文字光評審變異
  ≥0.33、跨學生路徑全幅 0.0↔1.0——**沒有任何 ε 能同時容忍此雜訊又抓得到真退步**。最終
  （人工授權）將 `judge_dialogue_*` 降為 **advisory**（計算＋質性審閱、不進硬性 pass/fail），
  對話數學正確性改由後盾把關；硬性守門 = 確定性 + 單輪評審（n≈13 穩定）+ altmethod + 後盾，
  四輪全數穩定通過。核心教訓：**輸入隨機的指標基準不可棘輪到幸運最大值。**
- **本機 6GB 卡工程教訓**：連跑兩輪 suite 間須 `ollama stop`（Ollama 駐留 3.2GB 會使下一輪
  4-bit 載入 segfault）；跑本機守門前須關 Antigravity IDE（背景進程搶 GPU 致 4-bit 載入
  segfault）。

---

## 9. 商品化實作（Productization）

將部署形態包裝為**本機單人 Gradio 介面**（`dataset/app.py`），供非技術使用者操作。

- **使用流程**：使用者貼上自己的證明題（可含目前的證明嘗試，亦可留空）→ 系統自我驗證備課
  （進度串流顯示 PROVER/VERIFIER/REPAIR/SEGMENTER）→ 建立 `TutorDriver` 逐步引導；
  備課驗證失敗自動走同學模式。使用者貼的半成品證明直接當「學生開場白」路由審閱；錯誤證明
  則由審閱後盾找碴、引導改正而不奉送。
- **架構決策**：模型常駐載入一次（4-bit 約 3.5GB VRAM）；備課於背景執行緒跑、以佇列把階段
  串流到 UI；單 GPU 佇列 `concurrency_limit=1` 序列化；`gr.State` 以參照持有 driver（實證
  Gradio 6 執行期不 deepcopy，模型安全）。
- **健壯性/UX 優化**：備課 worker `try/finally` 保證送結束哨兵（例外回報而非 UI 卡死）、
  `on_send` 即時回顯學生訊息＋生成期間顯示等待、進行中操作停用對應按鈕（防單 GPU 佇列
  被連點塞爆）。
- **驗證**：純邏輯單元測試（`test_app.py`）涵蓋備課回呼、組裝、opener 分流、Ollama 偵測、
  備課例外不卡死；`import app` 不載模型（測試不需 GPU）；Gradio 建構與伺服器服務煙霧測試
  HTTP 200。互動四情境驗收需真模型＋Ollama＋瀏覽器（部署端執行）。

---

## 10. 限制與未來工作（Limitations & Future Work）

- **模型級收尾紀律**：v10/v11 顯示 830 例 journey 重訓會讓模型在「完成後帶問句反思」時更傾向
  順勢延伸，偶伴數學錯誤；已由 driver `closed`/`done_closed`（#11/#12）大幅兜住，但訓練面
  根治尚未達成。方向：補「模型主動延伸→應收手」的負面對照樣本。
- **審閱解釋精確度**：「察覺對但解釋錯」是唯一 driver 蓋不住、需訓練面解法的環節。
- **備課依賴 Ollama 與延遲**：每題數分鐘、需思考型模型在線；6GB 卡上備課時 Ollama 會被擠到
  CPU 變慢。
- **對話類守門方法**：n=3 對話指標雜訊過大，現為 advisory；未來可改多輪聚合（中位數／多次
  rejudge 平均）再對基準。
- **商品化擴充**：目前為本機單人；往真正產品需備課可取消、多使用者隔離、帳號與雲端 GPU。
- **驗證器**：同源 4B 驗證器邊際效益不足；未來換非同源強驗證器可重啟 BoN 路線。

---

## 11. 結論（Conclusion）

本專題證明：在單張 6GB 消費級 GPU 的嚴苛限制下，透過**「模型＋確定性驅動程式＋預寫內容＋
幕後思考型驗證」的分工架構**，可讓 4B 小模型穩定扮演不洩題、分級引導、抗壓、且數學可靠的
蘇格拉底式證明家教。關鍵洞見是：**把小模型不擅長的事（離散決策、連續量校準、即時數學判斷）
系統性地移出模型、交給可控的程式與幕後強模型，模型只做它擅長的數學表達與語氣**。部署形態
在域內達 4.37、跨域 4.25、備課正確率 100%，並已落地為可供一般使用者操作的商品化介面。

---

## 12. 參考文獻（依大類）

> 說明：以下依研究大類列出各領域的代表性／奠基性文獻，作為本專題方法選擇的學理依據。

**A. 智慧型教學系統與蘇格拉底式教學（ITS / Socratic Tutoring）**
- Bloom, B. S. (1984). *The 2 Sigma Problem: The Search for Methods of Group Instruction as
  Effective as One-to-One Tutoring.* Educational Researcher.
- VanLehn, K. (2011). *The Relative Effectiveness of Human Tutoring, Intelligent Tutoring
  Systems, and Other Tutoring Systems.* Educational Psychologist.
- 對話式教學／家教資料集與方法（如 MathDial 等「引導提問」對話語料與行為遷移研究）。

**B. 大型語言模型與指令微調（LLMs / Instruction Tuning）**
- Qwen Team. *Qwen Technical Reports*（Qwen2/Qwen2.5/Qwen3 系列，本專題基底模型）。
- Ouyang, L., et al. (2022). *Training language models to follow instructions with human
  feedback (InstructGPT).* NeurIPS.
- Bai, Y., et al. (2022). *Constitutional AI: Harmlessness from AI Feedback.*

**C. 參數高效微調與量化（PEFT / LoRA / Quantization）**
- Hu, E., et al. (2021). *LoRA: Low-Rank Adaptation of Large Language Models.* ICLR.
- Dettmers, T., et al. (2023). *QLoRA: Efficient Finetuning of Quantized LLMs.* NeurIPS.
- Dettmers, T., et al. (2022). *LLM.int8(): 8-bit Matrix Multiplication for Transformers at
  Scale.*（bitsandbytes 量化基礎）

**D. 數學推理、思維鏈與自我一致性（Reasoning / CoT / Self-Consistency）**
- Wei, J., et al. (2022). *Chain-of-Thought Prompting Elicits Reasoning in Large Language
  Models.* NeurIPS.
- Wang, X., et al. (2022). *Self-Consistency Improves Chain of Thought Reasoning.* ICLR.
- Cobbe, K., et al. (2021). *Training Verifiers to Solve Math Word Problems (GSM8K).*

**E. 推理驗證與過程監督（Verification / Process Supervision / Best-of-N）**
- Lightman, H., et al. (2023). *Let's Verify Step by Step.* （過程級監督，本專題自我驗證備課
  與審閱後盾的學理呼應）
- Best-of-N 取樣 + 結果／過程驗證器之相關研究（本專題實驗性評估後判定邊際效益不足）。

**F. grounding 與抗幻覺（Grounding / Hallucination / RAG）**
- Lewis, P., et al. (2020). *Retrieval-Augmented Generation for Knowledge-Intensive NLP
  Tasks.* NeurIPS.
- 幻覺綜述與以參考文本 grounding 抑制幻覺之相關研究（本專題以題目專屬參考解為錨）。

**G. LLM 作為評審與對話評估（LLM-as-judge / Evaluation）**
- Zheng, L., et al. (2023). *Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena.*
- Liu, Y., et al. (2023). *G-Eval: NLG Evaluation using GPT-4 with Better Human Alignment.*
  （本專題據此設計對雜訊免疫、只升不降的回歸守門）

---

## 附錄 A：關鍵產出與檔案對應

| 類別 | 檔案／目錄 |
|---|---|
| 部署 adapter | `dataset/qlora_adapter_v9/`（權重不進 git） |
| 對話驅動程式 | `dataset/tutor_driver.py`（含 #11/#12 收尾修復） |
| 自我驗證備課 | `dataset/auto_reference.py` |
| 審閱後盾 | `dataset/review_backstop.py` |
| 資料集 | `dataset/src/`、`dataset/src_en/`、`train.jsonl`(747)／`val.jsonl`(83)、`problems.json`(50) |
| 路線對決報告 | `dataset/eval_out_final/FINAL_VERDICT.md` |
| 跨域／SVT／後盾／BoN／v10／v11 報告 | `dataset/eval_out_xdomain/*.md` |
| 回歸守門 | `dataset/regression_suite.py`、`regression_baseline*.json`、`regression_scores/` |
| 商品化介面 | `dataset/app.py`、`dataset/test_app.py` |
| 設計文件 | `dataset_plan.md`、`socratic_math_research.md`、`self_verified_teaching_design.md`、`docs/superpowers/` |

## 附錄 B：最終判定摘要

- **部署形態**：Qwen3-4B-Instruct-2507 + `qlora_adapter_v9`（4-bit nf4）+ TutorDriver（#11/#12）
  + 審閱後盾（Ollama 思考型，可降級）。
- **v10／v11 adapter**：判退封存（收尾過度延伸），維持部署 v9。
- **守門**：硬性 = 確定性 + 單輪評審 + altmethod + 後盾（穩定全綠）；對話類 n=3 降 advisory。
