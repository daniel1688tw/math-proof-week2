# 專案架構設計 — 蘇格拉底式高等數學證明引導助教

> 版本：2026-08-07 ｜ 對應分支：`feature/product-ui` ｜ 部署形態：`qlora_adapter_v9` + TutorDriver + 審閱後盾
> 本文件是**架構的權威來源**。`docs/notion/01-系統架構.md` 是它的精簡快照，
> `CLAUDE.md` 記錄逐次迭代的量測與教訓，`README.md` 面向使用者。

---

## 目錄

1. [設計問題](#一設計問題為什麼不是接一個大模型就好)
2. [三條設計鐵律](#二三條設計鐵律)
3. [系統全景](#三系統全景)
4. [備課層：auto_reference.py](#四備課層autoreferencepy)
5. [決策層：TutorDriver](#五決策層tutordriver)
6. [生成層：QLoRA 微調模型](#六生成層qlora-微調模型)
7. [旁路：審閱後盾](#七旁路審閱後盾混合架構)
8. [介面層](#八介面層)
9. [品質守門作為架構的一部分](#九品質守門作為架構的一部分)
10. [關鍵取捨紀錄](#十關鍵取捨紀錄)
11. [擴充點](#十一擴充點)
12. [已知架構缺口](#十二已知架構缺口)

---

## 一、設計問題：為什麼不是「接一個大模型」就好

目標行為是「**不給答案的證明家教**」。這件事看似只要一段 prompt，實際上有四個
prompt 壓不住的難點，每一個都在本專案的迭代中被實測撞到過：

| 難點 | 純 prompt 的失敗樣態（實測） |
|---|---|
| **透漏多少** | 「提示深度」是連續量。模型要嘛第一輪就把關鍵定理送出去，要嘛學生卡死五輪還在反問 |
| **何時換階段** | 學生交了草稿、要求答案、說「懂了」——模型自己判斷時序，經常錯過或提早 |
| **抗壓** | 被逼問「直接告訴我」時，未微調模型只會政策式拒絕（零教學前進，S3 得分 3.00）；思考型模型甚至曾把完整證明整段交出 |
| **收手** | 證明完成後仍熱心延伸到替代證法／推廣，並在即興延伸中產生新的數學錯誤 |

而反過來，**純微調**也解決不了另外兩件事：

- 4B 模型對「學生草稿哪裡有洞」的即時數學判斷不可靠（會出現「察覺對但解釋錯」與錯誤背書）
- 面對訓練集外的新題目，沒有可靠的參考解就無法保證引導方向正確

於是架構的形狀被這兩組限制夾出來：**把每件事交給最適合它的元件**。

---

## 二、三條設計鐵律

這三條是 v2→v6 六輪迭代 + v10／v11 兩次判退換來的，**任何新功能都必須服從**：

### 鐵律 1：離散決策交給程式碼

「何時升級提示、何時換階段、何時收尾」是離散的、可枚舉的、可測試的。
全部寫成確定性 Python（`TutorDriver`），**不讓模型自己判斷**。

推論：任何「靠模型自律」的設計都是缺陷。實測依據——收尾偵測原本靠正則比對助教散文，
8 種自然措辭只命中 2 種；改成確定性訊號（審閱輪回覆不含問句＝沒再要求修正）後才穩定。

### 鐵律 2：內容拿捏交給預寫內容

「提示要說多深」「審閱該挑哪個缺漏」是連續量，SFT 校不準。做法是把內容**預先寫好**
（參考解、分級提示梯、教學步驟），由程式決定何時注入。行為即完全受控。

推論：等級 2 的提示文字必須事先存在。內建 17 題來自手寫 `hint_ladders.json`；
使用者自帶題目由備課的 LADDER 階段生成——**沒有梯就是架構破口**（見 §4.2）。

### 鐵律 3：模型只負責數學與語氣

微調模型的職責被壓縮到最小：把「程式決定的意圖」用正確的數學術語與蘇格拉底語氣說出來。
它不決定給多少、不決定何時轉換、不決定對錯。

**混合架構延伸（2026-07-12）**：即時數學判斷（找學生草稿的缺漏）也交出去——
交給 Ollama 思考型模型；微調模型只把缺漏清單包裝成引導語氣。**判斷與說話分工。**

---

## 三、系統全景

### 3.1 三層 + 一個旁路

```
┌─────────────────────────────────────────────────────────────────────┐
│ 介面層     app.py（Gradio）│ interactive_turn.py（CLI）│ 你的程式    │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ problem dict + 學生訊息
┌───────────────────────────▼─────────────────────────────────────────┐
│ ① 備課層  auto_reference.py（Ollama 思考型，一次性，每題一次）        │
│    PROVER×3 → VERIFIER → REPAIR → SEGMENTER → LADDER                │
│    產出：reference_proof / teach_steps / hint_ladder / status        │
│    status=unverified → 整個下游切換成同學模式                        │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────────┐
│ ② 決策層  TutorDriver（phase / level 判定為純確定性 Python）          │
│    state → 決定 phase / level → 組裝 system prompt                  │
│    tutor 的 guide/respond_attempt 才呼叫 Thinking 驗證、注入診斷      │
│    生成後：防護鏈（洩漏／奉送／算式／稱讚／重複／回問保底）            │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ messages + system
┌───────────────────────────▼─────────────────────────────────────────┐
│ ③ 生成層  Qwen3-4B-Instruct-2507（4-bit nf4）+ qlora_adapter_v9      │
│    greedy 解碼、max_new_tokens=240                                   │
└─────────────────────────────────────────────────────────────────────┘
                            ▲
┌───────────────────────────┴─────────────────────────────────────────┐
│ ⊕ 旁路  review_backstop.py（Ollama Qwen3-4B Thinking）               │
│    正式全文 review 使用既有 _consult_backstop()；                    │
│    一般嘗試為 guide/respond_attempt，啟用 VTG 時在生成前驗證並注入診斷 │
│    Ollama 不在線 → 自動降級（回到單模型行為，不報錯）                 │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 一輪對話的完整時序

```
step(student_text)
 │
 ├─ 1. 語言重判（≥12 非空白字元才切；支援對話中途換語言；**教學期間不重判**）
 ├─ 2. 掃「上一則助教回覆」補 arm：
 │      · confirms_whole_proof_done → done_closed = True          （#12）
 │      · asks_for_full_writeup     → writeup_asked = True        （H5 型）
 ├─ 3. _detect_phase(student_text)  ← 優先序見 §5.2
 ├─ 4. tutor ∧ phase=guide ∧ action=respond_attempt ∧ REVIEW_BACKSTOP=1
 │      ∧ VERIFY_THEN_GENERATE=1 → Thinking 生成前驗證（診斷注入 prompt）
 │      其他 guide action 不呼叫；unavailable → 保留既有生成後審閱的安全降級
 │      clear 只是這一次嘗試未見缺漏，絕不改變 phase 或完成狀態
 ├─ 5. phase == review → _consult_backstop()（2–4 分鐘）
 │      其他輪 backstop_gaps = None（已成功的 guide/respond_attempt 診斷除外）
 ├─ 6. is_stuck 判定 → stuck_count += 1 或歸零
 ├─ 7. _walkthrough_transition()    ← 逐步教學狀態機
 └─ 8. _tutor_turn()
        ├─ level = min(stuck_count, 2)
        ├─ phase == walkthrough → _walkthrough_reply() 確定性組出，**不呼叫模型**、跳過防護鏈
        ├─ phase == review ∧ backstop_gaps == [] → REVIEW_PASS 確定性收尾
        ├─ 否則 _system(level) 組裝（優先序：phase > level）→ 生成
        └─ 防護鏈（§5.6）→ 回傳
```

---

## 四、備課層：`auto_reference.py`

### 4.1 設計原則：先自己證對，才有資格教

助教面對新題目時，**不假設自己會**。五階段管線把「會不會」變成一個可判定的狀態：

| 階段 | 輸入 | 輸出 | 失敗處置 |
|---|---|---|---|
| **PROVER** ×3 | 題目 | 3 份獨立候選參考解（temperature 分散） | — |
| **VERIFIER** | 題目 + 一份候選 | `{verdict, issues[]}`；逐步檢查前提、量詞、不等號方向 | 該份淘汰，換下一份 |
| **REPAIR** | 題目 + 候選 + issues | 修補後的證明 → 再驗一次 | 再驗不過即淘汰 |
| **SEGMENTER** | 題目 + 通過的證明 | 3–6 個教學步驟，每步 `{step_id, explain, check, expected_answer, accepted_answers, common_errors}`；過 `validate_teach_steps` 五道確定性驗收 | **整份丟棄**，改走句級保底 `fallback_steps`（3–6 步、每步都有答案鍵）|
| **LADDER** | 題目 + 通過的證明 | 恰 2 條分級提示 | **整份丟棄，不寫 `hint_ladder` 鍵** |

三份候選全部沒過 → `status = "unverified"` → 下游切同學模式。

**盲測結果**（10 題已知解、隱藏答案盲跑）：verified 9/10，**這 9 題的參考解正確率 9/9
＝零錯誤背書**。唯一沒過的是歷來最難的 Darboux 題——「知之為知之」正是設計目標，
不是失敗。

### 4.2 LADDER 階段：為什麼它是架構必需品而非加分項

沒有它，鐵律 2 在「使用者自帶題目」這條路徑上完全失效。實測退化：

| 連續卡住 | 有梯 | 無梯 |
|---|---|---|
| 1 | 等級 1 | 等級 1 |
| 2 | 等級 2，注入 `ladder[0]` | 等級 2，注入**通用 meta 指示** |
| 3 | 等級 2，注入 `ladder[1]` | **進 walkthrough** |
| 4 | 進 walkthrough | — |

兩個傷害：等級 2 的透漏內容退化成「點出關鍵定理或想法名稱」這種要模型自己拿捏的指示
（正是鐵律 2 要避免的）；而且因為 `ladder_len = max(len(ladder), 1)`，**提早一輪**掉進
逐步教學——該被第二條提示救起來的學生直接被講答案。

**prompt 規格由 33 條手寫提示反推**：恰兩條、依序對應證明的兩個關鍵轉折、每條點名一個
定理／構造／性質的名稱與作用、不得出現任何算式。

**五道確定性驗收**（`validate_ladder`，全部對 33 條手寫提示校準，實測零誤判）：

1. 恰 2 條非空字串
2. 每條 12–60 字
3. `gives_new_equation(h, statement)` 為 False
4. `leaks_reference(h, proof, exclude=statement)` 為 False
5. 兩條 difflib 相似度 < 0.85

> 第 3 道是**安全關鍵**：等級 2 的禁算式白名單 `_allowed_equation_src()` 包含當前提示文字，
> 提示若帶算式會讓該守衛對那些算式失效。
> （曾考慮把 hint 移出白名單當更根本的防線，但 ADV1 的手寫提示**刻意**帶算式並依賴
> 白名單放行，移除會打壞既有題目——故改為「讓生成的梯保證無算式」。）

### 4.3 降級設計：所有失敗路徑收斂到同一結果

Ollama 離線、無法解析、驗收不過——三種失敗都**不寫 `hint_ladder` 鍵**（不是寫 `None`），
`TutorDriver._ladder()` 讀不到就回退通用保底句，等於 LADDER 上線前的行為。
**最壞情況零退步**是這個管線的設計約束，不是巧合。

`build_reference(statement, k=3, progress_cb=None)` 的 `progress_cb` 是選填回呼，
UI 靠它做逐階段串流——**向後相容，不加也能跑**。

---

## 五、決策層：`TutorDriver`

全檔 1536 行，**沒有任何隨機性**（生成走 greedy），因此可以離線回放與斷言。

### 5.1 狀態欄位

| 欄位 | 型別 | 語意 | 生命週期 |
|---|---|---|---|
| `lang` | `"zh"`/`"en"` | session 語言，逐輪依學生訊息重判（**教學期間不重判**） | 每輪可變 |
| `stuck_count` | int | **連續**卡住次數 → `level = min(_, 2)` | 不卡住即歸零 |
| `ladder_idx` | int | 下次等級 2 要用第幾條提示 | **只在提示真的送出時 +1** |
| `phase` | str/None | 本輪階段（見 §5.2） | 每輪重算 |
| `writeup_asked` | bool | 已請學生寫完整證明 | **單向閂**（誤設會反噬） |
| `done_closed` | bool | 整份證明已確認完成 | 單向閂 |
| `walk_active` / `walk_idx` / `walk_retry` | bool/int/int | 逐步教學狀態機 | 教完歸 False |
| `walk_lang` | `"zh"`/`"en"`/缺 | 教學期間**鎖定**的語言；存在時蓋過 `lang` | 進入時設、教完刪 |
| `walk_presented_step` / `_idx` / `_step_id` | dict/int/str | 上一輪**實際呈現**的那一步＝下一輪的評分對象 | 前進或收尾時清空 |
| `walk_feedback` | str | 待附在下一則教學回覆前面的回饋／答案揭示句 | 用掉即 pop |
| `teach_steps_source` | str | 步驟來源：`segmenter` / `fallback` / `paragraphs` | 生成時寫一次 |
| `fb_idx` | int | 回問保底句輪換索引 | 只增 |
| `backstop_gaps` | list/None | 後盾缺漏清單；`None` = 未啟用 | 每輪重設 |
| `pre_generation_verification` | dict | `respond_attempt` 生成前驗證的 status / issues / latency 診斷 | 每輪重設；僅可觀測與 prompt grounding |
| `_regens` | int | 本輪已用重生成次數（上限 `_MAX_REGEN_PER_TURN=2`） | 每輪歸零 |
| `turns` | list[TurnLog] | 逐輪 `level / stuck_count / guards / regenerated` | 累積 |

`TurnLog.guards` 是**可觀測性的核心**：守門與回放測試都靠它斷言「這一輪哪些防護動了」。

### 5.2 階段機：`_detect_phase`

優先序是刻意的（**由上而下，先命中先決定**）：

```
0. is_peer()  → phase = peer_reflect（被質疑）或 None，同儕沒有階段機
1. _DRAFT_RE                                    → review           交草稿
2. writeup_asked ∧ len≥120 ∧ ¬要求 ∧ ¬致謝      → review           交稿後的長訊息＝證明本體
3. _CLAIM_DONE_RE ∧ len≥80 ∧ ¬致謝              → review           口頭交稿（不 arm done_closed！）
4. _DEMAND_RE                                   → refuse_leak      逼問答案
5. _ATTEMPT_RE                                  → rectify          交嘗試
6. _UNDERSTOOD_RE ∧ ¬writeup_asked              → writeup_request  說懂了
7. done_closed ∧ ¬_CHALLENGE_RE                 → closed           收尾
8. 其餘                                          → None             一般引導輪
```

**第 3 條的「不 arm」是血的教訓**：學生說「得證」不等於證明成立。交稿當下就 arm，
會讓他針對缺漏的追問被路由進 `closed`（助教被指示「已完成、不要再問」）而**中斷糾錯**。

**第 7 條的 Fork B 決策**（2026-07-23 使用者定案）：完成後帶問句的反思一律進 `closed`
簡答，只有**斷言式質疑**（「你錯了」）才落回一般流程重查。理由：已完成的證明必經
review＋後盾複核，非斷言的「你確定嗎」簡答即可。

### 5.3 等級機：單調遞增是不變式

```
stuck_count:  0    1    2    3    4  ...
level:        0    1    2    2    2       ← min(stuck_count, 2)
ladder_idx:   0    0    1    2    2       ← 只在 level==2 ∧ phase is None ∧ ¬peer 時 +1
```

兩個曾經被破壞、現已固化為測試的不變式：

- **等級不得回退**：曾在送出等級 2 提示後把 `stuck_count` 歸零，導致序列
  `0→1→2→1→2→1`——對**持續卡住**的學生，支援強度在 2↔1 之間震盪而非單調遞增
  （等級 1 的指示是「拆更小的子問題、仍不點名定理」＝給得**更少**）。
- **提示梯守恆**：`phase` 有值時 system 注入的是階段指示、根本沒有提示內容，
  卻曾照樣推進 `ladder_idx` → 提示被消耗但學生從未看到；更糟的是 walkthrough 的
  進入條件就是 `ladder_idx >= 梯長`，**一串糾錯輪就能把梯吃光**。

### 5.4 逐步教學狀態機

```
進入條件（三者皆須成立）：stuck_count ≥ 2  ∧  ladder_idx ≥ ladder_len  ∧  phase is None
   │  進入時**鎖定語言**（walk_lang），教學期間 step() 不再重判 session 語言
   │
   ├─ 每輪**確定性**輸出「第 i/n 步：{explain}\n\n確認問題：{check}」，不呼叫生成模型
   │   └─ 並記下 walk_presented_step ＝這一輪實際呈現的是哪一步（下一輪的評分對象）
   ├─ 評分 grade_walkthrough_answer(學生訊息, walk_presented_step)
   │   ├─ correct   → walk_idx += 1、walk_retry = 0
   │   ├─ incorrect / stuck → 留在同一步，walk_retry += 1，下一輪加前綴重講
   │   │                      （重講輪是這階段**唯一**會叫生成模型的地方：請它換說法；
   │   │                        換不出／不在線／推託「你自己去查」→ 退回模板＋補該步答案）
   │   └─ walk_retry > _MAX_WALK_RETRY(=2) → 揭示 expected_answer 後強制前進
   │       （安全閥：答案鍵是自動生成、無人工審過的，寫壞時不能讓學生死鎖）
   ├─ 學生交草稿 → 隨時可打斷進審閱，但教學期間走**專用路由**：
   │   只有 _EXPLICIT_REVIEW_RE 或整則以「證明：」開頭才算交稿
   │   （第 1 步的標準答案常長成「要證明：對任意 a<b…」，走一般 _DRAFT_RE 會被誤判）
   └─ walk_idx ≥ len(steps) → walk_active=False, phase=writeup_request
       （最後一步的答案揭示句留在 walk_feedback，由下一輪帶出去）
```

**關鍵設計 1：教完仍要學生自己寫證明。** 逐步教學是幫他跨過門檻，不是代寫。

**關鍵設計 2：這一階段不呼叫生成模型（2026-08-07）。** 內容本來就是預寫的
（設計鐵律：內容拿捏交給預寫內容），而這裡連語氣都不值得賭——實測模型會把同一步
逐字重講三輪、一次講掉好幾步、把確認問題換掉使答案鍵無從比對。附帶效果是把
教學輪每輪最多 4 次的重生成降為 0（守門該段 45 → 12 分鐘，學生每輪等待同步縮短）。
代價是語氣固定，這是拿語氣換可預測性的自覺取捨。

⚠️ 目前已知的兩個缺口見「已知弱點」#20（`common_errors` 會把正確答案判成錯）與
#21（重講輪的防重複比對抓不到逐字重講），皆已有實測輸入，見
`docs/code-review-walkthrough-gradable.md`。

### 5.5 system prompt 組裝優先序

```
phase == walkthrough ──► **完全不進 _system()**：由 _walkthrough_reply() 確定性組出回覆
     │                    （唯一例外是重講輪，它用 BASE_SYSTEM + WALK_RETRY_INSTRUCTION）
     │
is_peer() ──► PEER_SYSTEM (+ PEER_REFLECT_INSTRUCTION 若被質疑)      ← 無參考解，同儕 persona
     │
     └─ 否則：BASE_SYSTEM.format(proof=reference_proof)   ← grounded，含 <REFERENCE_PROOF>
             + 下列**擇一**：
               phase ∈ PHASE_INSTRUCTIONS → 階段指示（review/rectify 或 `respond_attempt` 可另接後盾缺漏清單）
               level == 2            → LEVEL_INSTRUCTIONS[2].format(hint=ladder[idx])
               其餘                   → LEVEL_INSTRUCTIONS[level]
```

**階段指示優先於等級指示**——這是 §5.3「提示梯守恆」缺陷的成因，理解這條優先序才理解
為什麼 `ladder_idx` 只能在 `phase is None` 時推進。

### 5.6 防護鏈：順序即設計

生成後的處理順序**不是任意的**，每一步的位置都有實測理由：

| # | 防護 | 條件 | 動作 | 為什麼在這個位置 |
|---|---|---|---|---|
| 1 | `enforce_single_question` | 非教學輪 | 截斷到第一個問句 | 教學輪本就是「講解＋確認」多句結構 |
| 2 | `writeup_request` 保底 | 模型沒請學生寫證明 | 直接換模板 | 階段語意不可協商 |
| 3 | **`_content_guards`** | 有參考解 ∧ 非教學輪 | 見下 | — |
| 3a | 洩漏偵測 | `level<2` ∧ 15-gram 命中參考解 | 重生成 | `exclude=statement`（複述題幹不算洩漏） |
| 3b | on-track 防奉送 | `level<2` ∧ `phase ∈ {None, refuse_leak}` | 重生成 | 等級 2 本來就該透漏 |
| 3c | 等級 2 禁算式 | `level==2` ∧ 白名單外的新等式 | 重生成 | 白名單 = 題目 + 學生訊息 + 當前提示 |
| 3d | 稱讚校準 | 後盾報了缺漏卻無條件背書／誇飾腔 | 重生成 | 需要 `backstop_gaps`，故排在後盾之後 |
| 4 | 同儕背書守衛 | `is_peer()` ∧ 總評式肯定 | 重生成 | `_content_guards` 對 peer 停用，這是它的同儕版 |
| 5 | 重複偵測 | 與近 3 輪回覆 difflib ≥0.85 | 重生成 **+ 複驗** | **必須早於回問保底**（否則會蓋掉剛補的問句） |
| 6 | arm `done_closed` | 本輪親口宣告整份完成 | 設旗標 | **必須早於回問保底**（否則同一則回覆自打嘴巴） |
| 7 | 回問保底 | 需要問句卻沒有 | 重生成 → nudge/fallback | 最後一道，確定性補救 |
| 8 | 同學模式首輪聲明 | peer ∧ 首則回覆 | 前置 disclaimer | 不賭模型自己說 |
| 9 | arm `done_closed`（審閱通過） | `phase==review` ∧ 回覆無問句 | 設旗標 | 確定性訊號，不比對散文 |
| 10 | `ladder_idx += 1` | `level==2` ∧ `phase is None` ∧ ¬peer | 推進 | 見 §5.3 |

**最重要的一條不變式：每一次重生成的結果都必須再過一次 `_content_guards`。**
這曾是全檔唯一未經內容檢查就送到學生面前的路徑——實測可讓一段逐字複製參考解、
結尾帶問號的回覆完整落地，正好架空招牌的抗洩漏防護。

**重生成預算**：`_MAX_REGEN_PER_TURN = 2`。教學輪每輪最多 4 次重生成曾讓守門該段
由 12 分鐘漲到 45 分鐘，**學生每輪等待同步變成 3–4 倍**。配額用盡就不再賭模型服從，
直接走確定性補救。

### 5.7 狀態快照：`dump_state` / `load_state`

逐輪 CLI 每輪都是新行程，需跨行程還原。**整包存（而非逐欄列舉）是刻意的**：
舊版只存 4 個欄位，於是 `done_closed` / `fb_idx` / `walk_*` 每輪歸零——收尾修復、
保底句輪換、逐步教學進度在 CLI 上**全部失效**。整包存之後，新增狀態欄位自動跟著存。

`app.py` 因 driver 常駐記憶體不受此影響。

---

## 六、生成層：QLoRA 微調模型

| 項目 | 值 |
|---|---|
| 基底 | Qwen3-4B-Instruct-2507 |
| 量化 | 4-bit nf4（`device_map="auto"`） |
| Adapter | `qlora_adapter_v9`（`FINAL_ADAPTER` 可覆寫） |
| 訓練 | `MAX_LEN=1024, EPOCHS=3, GRAD_ACCUM=8, OPTIM=adamw_8bit, NEFTUNE_ALPHA=5` |
| 解碼 | greedy，`max_new_tokens=240` |

**`MAX_LEN=1024` 是 v9 的關鍵改動**：640 會截掉 26% 訓練樣本的**尾端**——正是對話收尾／
審閱輪所在的位置，也正是「完成後仍多講」這個弱點的訓練面根因。

**greedy 解碼是架構決定，不是省事**：同輸入同輸出，使守門能做「本輪與上輪回覆逐字比對」
——全同即證明生成端零變化，所有評審分數波動都是雜訊（見 §9.3）。

**微調的價值在哪**（8 題 × 3 情境對決）：

| 路線 | S1 首問 | S2 糾錯 | S3 逼問 | 平均 |
|---|:---:|:---:|:---:|:---:|
| **微調 + driver + 後盾** | 4.31 | 4.50 | 4.31 | **4.37** |
| 微調 + driver（後盾離線） | 4.31 | 4.19 | 4.31 | 4.27 |
| 未微調基底 + 同 prompt | 3.88 | 4.06 | **3.00** | 3.65 |
| Ollama 思考型 + 同 prompt | 3.63 | **4.75** | 3.63 | 4.00 |

差距集中在 **S3 逼問**（3.00 vs 4.31）：未微調模型只會政策式拒絕（零教學前進）。
思考型模型的 S2 4.75 很亮眼——這個觀察直接催生了 §7 的混合架構。

**訓練資料**：50 題手寫參考解 + 中英平行多輪對話（`train.jsonl` 747 / `val.jsonl` 83）。
全部由人撰寫並對照參考解驗證，**不是叫小模型生成的**。

> ⚠️ 現役 v9 訓練於 **814 例**的版本（733/81）；目前 `build.py` 產出的 830 例含後加的
> `dialogues_journey.py`（v10／v11 用，兩次整體重訓皆判退）。**資料集狀態 ≠ 部署模型的訓練狀態。**

---

## 七、旁路：審閱後盾（混合架構）

### 7.1 為什麼要有它

微調模型審閱草稿時的失敗樣態是「察覺對但解釋錯」與**錯誤背書**（把有雙重錯誤的草稿
說成「平均式子沒問題」）。而對決數據顯示 Ollama 思考型模型的 S2 糾錯是全場最高的 4.75。

於是：**讓思考型模型只在幕後找碴、永不直接面對學生**（它的致命傷——S3 會把完整證明
整段交出、偶有空輸出——因此被隔離）。

### 7.2 機制

```
phase ∈ {review, rectify}
   └─ find_gaps(statement, reference_proof, student_draft)   ← Ollama qwen3-4b-thinking-2507
        └─ 缺漏清單（按嚴重程度排序）
             └─ 組成 [複核結果] 段落注入 system
                  └─ 微調模型：「挑第一項，用一個問題引導學生自行發現並修正；
                     不要提清單以外的問題，也不要把正確版本直接講完」
```

清單為空時注入的是**反向指示**：「未發現缺漏。若你也同意，直接肯定學生，
不要憑空發明問題。」——防止後盾變成無中生有的找碴機。

### 7.3 效果與工程細節

- S2 4.19 → **4.50**（跨域 4.67 → 4.92），S1/S3 **逐字不變**；H3 的錯誤背書消除。
- 找碴準確度 3/3 精準。關鍵是 critic prompt 要求「**教學標準**」——未明說的依據也算缺漏；
  用數學家標準會放行。
- 思考鏈需 `num_predict=8192`（3072 會被思考吃光、正文空白）。
- 模型輸出的 LaTeX（`\{` `\dots`）是**非法 JSON escape**，解析需反斜線加倍重試。
- 每次找碴 2–4 分鐘（GPU 被 HF 佔用時後盾走 CPU）。

### 7.4 降級是一等公民

`ImportError` / Ollama 離線 → `backstop_gaps = None` → `_backstop_block()` 回空字串 →
回到單模型行為。`REVIEW_BACKSTOP=0` 強制關閉。**不報錯、不中斷對話。**

### 7.5 生成前驗證：最小整合，不改階段語意

`VERIFY_THEN_GENERATE=1`（預設）時，只有 tutor 模式的 `guide/respond_attempt` 在 Qwen3-4B
+ `qlora_adapter_v9` 生成前呼叫同一個 Qwen3-4B Thinking/Ollama verifier。它把 `issues` 或
`clear` 診斷寫入 `pre_generation_verification`，並只把結果作為本輪 prompt grounding；其他
guide action 不負擔這次呼叫。`REVIEW_BACKSTOP=0` 是全部後盾功能的總開關，故也會停用此驗證；
設 `VERIFY_THEN_GENERATE=0` 則回退既有 `full-project` 流程。

這個 verifier **沒有權限**發出 `READINESS_PASSED`、`REVIEW_PASSED` 或任何 phase transition。
尤其 `clear` 只表示本次局部嘗試未見缺漏，不是證明完成的證據；完成／審閱通過仍只可由既有的
readiness judge 與 review 工作流決定。verifier unavailable 時記錄狀態後不注入診斷，安全回退至
原有生成與生成後審閱，不中斷對話。

---

## 八、介面層

### 8.1 `app.py`：Gradio 本機單人 Demo

三層結構，**可測性是分層的理由**：

| 層 | 內容 | 測試 |
|---|---|---|
| 純邏輯 | `check_ollama` / `assemble_problem` / `opener_for` | `test_app.py`（無 GPU、不連 Ollama） |
| 備課串流 | `_run_prepare`：背景執行緒 + `queue`，worker `try/finally` 保證送出結束哨兵 | `test_app.py`（含例外不卡死） |
| UI 組裝 | `build_ui`：Blocks / 事件綁定 | 手動驗收 |

健壯性設計：

- **`try/finally` 哨兵**：備課拋例外時 UI 顯示錯誤並留在輸入態，不會永久卡死。
- **同時停用 `send_btn` 與 `msg_tb`**：只停用按鈕的話，按 Enter 照樣送出，
  單 GPU 佇列會被塞爆。
- **`demo.queue(default_concurrency_limit=1)`**：這是單張 6GB 卡的 Demo，序列化處理。
- **綁 `127.0.0.1`**：只接受本機連線。

題目 id 固定 `USER`，**完全不查題庫**——使用者自帶題目是這個介面的定位。

### 8.2 `interactive_turn.py`：逐輪 CLI

每輪一個新行程，靠 `--state session.json` 還原（§5.7）。也提供 `load_model()` 供 `app.py` 複用。

---

## 九、品質守門作為架構的一部分

守門不是附屬品——它是唯一能證明「改動有沒有真的改善」的機制，因此列入架構。

### 9.1 五層

| 層 | 內容 | 判定 | GPU |
|---|---|---|---|
| **Tier 0** | 資料集驗證、`test_driver_unit.py`（313 條 / 32 組）、`test_phase_routing.py`（299 場回放） | 零容忍 | ✗ |
| **Tier 1** | 單輪確定性探針：洩漏／拒絕／單問句／提示升級／教學收尾 | 零容忍 | ✓ |
| **Tier 1b** | **多輪確定性探針** `mt_*`：多輪不洩漏／單問句／階段流／提示梯守恆／乾淨收尾／糾錯不中斷 | 零容忍 | ✓ |
| **Tier 2** | 單輪評審 `judge_math_ok / score / s2_catch / reveal_ok / altmethod`（n≈13） | 容忍 ε | ✓ |
| **Tier 3 / 4** | 多輪對話評審（評審扮學生）、後盾找碴準確度 | **advisory** | ✓ |

評審後端預設 `antigravity`（`agy` / Gemini 3.6 Flash Medium），`JUDGE_BACKEND=claude` 為備援。
**不同裁判的尺不可互比**，故基準檔獨立（`regression_baseline_antigravity.json`）。

### 9.2 兩個曾經的結構性盲區

- **手寫單元測試證明不了真模型的話會不會踩中正則**。收尾／交稿／完成宣告偵測全在比對
  散文，實測 8 種自然措辭只命中 2 種。→ `test_phase_routing.py` 把 299 場**真實**守門
  對話回放進確定性層，檢查 5 條不變式。
- **Tier 1/2 是單輪探針，測不到多輪行為**。連續 6 輪守門、期間改了守衛鏈順序與
  `is_stuck` 語意，104 筆回覆**始終逐字相同**。→ Tier 1b。首度跑完就抓到弱點 #19。

> **教訓：`test_phase_routing.py` 的 V4 初版是空的**——它用一個「排除帶問句回覆」的
> 函式做判斷，該函式本身永遠不可能觸發。**每條不變式都必須實際還原一次 bug 驗證會響。**

### 9.3 守門自己的雜訊（判讀結果前必讀）

`measure_gate_noise.py`：取 14 輪同後端、Tier 1/2 回覆**逐字全同**的守門（生成端零變化），
差異即評審自身的不確定性。

- **9 個硬性 judge 指標中有 8 個的純雜訊 ≥ 自己的 ε**（`s2_catch_zh` 0.159、
  `score_zh` 0.135、`reveal_ok` 0.115…），只有 `math_ok_zh`（0.0385）達標。
- ⚠️ **放寬 ε 不是解法**：`s2_catch_zh` 共 14 題，真實「多錯 2 題」= 0.143 比雜訊 0.159 還小，
  任何蓋得住雜訊的 ε 都會同時放行真實退步。
- 處置：最不可靠的降 advisory（`judge_dialogue_*`、`dialogue_ladder_used_*`、`judge_backstop`）；
  Tier 2 改多次評審取共識（`JUDGE_SAMPLES=3`，效果**尚未證實**）。

**由此得到的紀律**：判退時的第一個動作是**逐字比對本輪與上輪的 `*_replies.json`**。
全同 ⇒ 生成端沒變 ⇒ 所有 judge 波動都是雜訊。任何採用／判退決策都不可只用 judge 分數當依據
（v10 判退是靠「質性內容跨四輪一致」認定的）。

### 9.4 基準管理

只升不降；更新時**逐指標取高**（保留較高的舊地板）。**輸入隨機的指標不可棘輪到幸運最大值**
（Tier 3 對話由 LLM 即興生成，連學生生成失敗都會讓指標歸零）——應設在可辯護的真實水準。

---

## 十、關鍵取捨紀錄

> 完整判定見 `CLAUDE.md` 與 `dataset/eval_out_*/`；此處只列**影響架構形狀**的決策。

| 決策 | 選擇 | 理由 |
|---|---|---|
| 提示深度由誰決定 | **預寫內容 + 程式注入** | SFT 校不準連續量（v4→v6 三次驗證） |
| 草稿找碴由誰做 | **思考型模型（旁路）** | 4B 微調模型會錯誤背書；思考型 S2 4.75 |
| 思考型模型可否直接面對學生 | **不可** | S3 曾把完整證明整段交出、有空輸出 |
| 收尾判定訊號 | **審閱輪回覆無問句** | 措辭比對 8 種只中 2 種 |
| 完成後帶問句的反思 | **一律進 closed 簡答**（Fork B） | 已完成的證明必經 review + 後盾複核 |
| 提示梯生成失敗 | **整份丟棄** | 所有失敗路徑收斂到同一結果 ⇒ 最壞情況零退步 |
| BoN + 驗證器 | **不採用**（`VERIFY=0`） | 實戰 273 輪僅 1 輪真的抓錯（0.4%），多 8GB 顯存與延遲 |
| v10 / v11 adapter | **不升版** | 收尾紀律退化跨四輪一致重現（非評審雜訊） |
| `dialogues_journey.py` | **保留不上線** | 兩次判退共同指向它的「連續多輪深入追問」訓練訊號 |
| 雙語備課 | **不擴充** | 只生繁中梯；英文 session 拿到混語提示，4B 上實際傷害可控 |

---

## 十一、擴充點

### 加一道內容防護

寫成 `is_xxx(reply, ...) -> bool` 純函式（可單測），在 `_content_guards()` 內加一段
`if 命中: log.guards.append("名稱"); reply = self._regen(level, 中英雙語指示)`。
**放在 `_content_guards` 內**即自動享有「每次重生成後重跑」的保證。

### 加一個階段

1. `PHASE_INSTRUCTIONS` / `PHASE_INSTRUCTIONS_EN` 加指示
2. `_detect_phase` 加分支——**注意優先序**（§5.2），先命中先決定
3. 檢查是否該進 `ladder_idx` 推進的排除集（§5.3）
4. `test_driver_unit.py` 加一組；`test_phase_routing.py` 若有對應不變式一併加

### 加一個語言

現行雙語靠「所有 prompt 常數的 `_EN` 對應版 + `lang` property 選擇」。加第三語需要：
`detect_lang` 擴充、每個常數的第三份、`_ladder()` 的回退順序、以及守門的 `_LANG_STR`。
**成本高，目前不建議**。

### 換評審後端

`regression_suite.py` 已有抽象層（`_judge_cmd` / `claude_call`）。**新後端必須先做選型驗證**：
壓測可解析率、判準對照（故意給一對一錯）、同輸入重跑穩定性。
教訓：**模型層級高 ≠ 當裁判可靠**（Gemini 3.1 Pro 對話層兩輪 0.333↔0.0 崩潰，反而淘汰）。

---

## 十二、已知架構缺口

按優先序，**這些是 driver 補丁蓋不住的**：

| # | 缺口 | 為什麼架構蓋不住 | 可能方向 |
|---|---|---|---|
| **#20** | **`common_errors` 排在答案鍵之前比對，會把正確答案判成錯** | 逐步教學的評分是確定性的，但錯答清單用**無長度下限的子字串**比對且排在 `expected_answer` 之前：`expected="非負"` ＋ `common_errors=["負"]` 時，答對永遠先命中 incorrect（實測） | 先比對答案鍵、沒命中才查 `common_errors`；套用短答保護 |
| **#21** | **教學重講的防重複比對抓不到「逐字重講」** | 比對對象是上一則**完整回覆**（含模板前綴與確認問題），受測文字只有 explain 本體 → 逐字吐回只算 0.845 < 0.9（實測）。與弱點 #17 第一批修復同型 | 改比對 `step["explain"]`，或先剝掉模板 |
| **#22** | **備課端與 driver 端的語言判定不一致** | `auto_reference._detect_lang` 沒跟上 `_strip_language_neutral_math` 的強化，同一段中文 driver 判 zh、備課判 en（實測）→ 中文講解配英文確認問句 | 共用同一個剝除函式；`build_reference` 把語言算一次往下傳 |
| **#19** | **英文多輪對話會洩漏參考解（4 題中 3 題）** | Tier 1b 首度跑完即抓到，尚未修。單輪 `s3_refusal_en` 長期 1.0——**「單輪不洩漏」推不出「多輪不洩漏」** | 下輪優先；先定位是防護漏判還是多輪上下文稀釋 |
| **#15** | **對話中途的錯誤步驟完全沒有檢查** | 後盾只在 `phase ∈ {review, rectify}` 觸發；學生推導途中寫錯一步（沒說「這樣對嗎」）時 `phase=None`，模型手上沒有任何「這步錯了」的訊號，照 `BASE_SYSTEM` 的「方向正確就肯定」去肯定 | 學生訊息含算式即呼叫後盾複核；代價是該輪多 2–4 分鐘 |
| **#16** | **助教自己的數學錯誤沒有任何攔截點** | 六道防護沒有一道檢查**內容正確性**。BoN + 驗證器已判定不採用 | 換非同源強驗證器才值得重啟 |
| **A** | 審閱時的**解釋精確度**（察覺對但解釋錯） | 唯一還會出現數學錯話的環節；屬生成內容品質，driver 無從判定 | 訓練面；或把解釋也交給後盾產出 |
| **G** | 備課的**獨立性**：證明者與驗證員是同一個 4B 思考模型的兩次獨立取樣 | 非獨立模型 ⇒ 共同盲點無法被抓出 | 換非同源驗證模型；新領域先抽查 verified 解 |
| — | **自動生成的提示梯只有安全性驗收** | 五道閘擋洩漏／算式／退化／格式，擋不掉「講得爛但無害」；且守門全走內建題，**這條路徑在指標上隱形** | 人工抽查；或加語意層驗收 |
| — | **逐步教學在子目標間跳躍** | 重複迴圈修好後評分回升，但評語指向「未走完一個子目標就跳下一個」 | 教學步驟的依賴關係需顯式建模 |

### 已知的環境層問題（非架構缺陷）

- **連續 2 小時守門後，walkthrough 探針處出現進程層死結**（兩次重現、同一位置）。
  判定依據：CPU 時間 40–45 秒內只增加 0.0–0.1 秒、49 執行緒全靜止、零網路連線
  ⇒ 排除 Ollama、疑似 CUDA/PyTorch 層。**排除方式**：`Stop-Process -Force`，讓機器休息後再跑。
- **本機 6GB 卡**：連跑兩輪 suite 之間必須 `ollama stop`（後盾模型駐留 3.2GB 會讓下一輪
  4-bit 載入 segfault）；跑守門前先關 Antigravity IDE（16 個背景進程搶卡）。

---

## 附錄：檔案 → 架構層對照

| 檔案 | 層 | 行數 | 動到要跑 |
|---|---|---:|---|
| `dataset/tutor_driver.py` | ② 決策層 | 1195 | `test_driver_unit.py` + `test_phase_routing.py` + 完整守門 |
| `dataset/auto_reference.py` | ① 備課層 | 347 | `test_auto_reference.py`（需 Ollama，~1.5hr） |
| `dataset/review_backstop.py` | ⊕ 旁路 | 109 | `test_backstop.py`、`eval_backstop_e2e.py` |
| `dataset/app.py` | ⑧ 介面 | 217 | `test_app.py` + 瀏覽器手動驗收 |
| `dataset/interactive_turn.py` | ⑧ 介面 | 122 | 手動 |
| `dataset/hint_ladders.json`(`_en`) | ② 的預寫內容 | 17 題 | 完整守門 |
| `dataset/regression_suite.py` | ⑨ 守門 | 1024 | 自身 `--quick` |
| `learn_path/socratic_tutor/train_qlora.py` | ③ 訓練 | — | 重訓 → 完整守門 |
