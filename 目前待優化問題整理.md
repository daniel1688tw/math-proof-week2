# 目前待優化問題整理

> **狀態更新（2026-08-31）：本文是 2026-08-25 的歷史盤點。** 其中 P0 的學生所有權、
> 無問號橋接釐清、正向承接與 readiness 分離已在現行程式及單元測試落地。
> 最新待辦以 `docs/code-review-level-phase-2026-08-31.md` 為準。

- 對應版本：`D:\專案備份\目前最成功的版本!!! - 處理level優化 - 上兩版 - 備份`
- 整理日期：2026-08-25
- 主要依據：最新自訂微積分證明題的 17 輪測試對話與 phase／guide review 診斷
- 原則：修法必須適用所有微積分證明題；不新增題型專屬關鍵詞清單、不建立複雜 proof graph，也不增加不必要的模型呼叫。

## 一、結論與修正順序

目前最優先的不是繼續收緊 Level prompt，也不是再處理 JSON schema、token 或 timeout。真正影響教學品質的是：

1. 學生進度與所有權的判定仍混入 Tutor 歷史。
2. Router 會漏判長篇正確推導與明確的局部釐清問題。
3. guide 審查器缺少「是否真正承接最新學生訊息」的硬性條件。
4. guide 審查器仍會漏掉錯號、錯誤數學陳述及錯誤的學生所有權歸因。
5. `level_policy`、候選回覆品質與 readiness 的理由仍會互相污染，導致不必要的重生成與脫離情境保底句。

建議依上述順序修正。先讓系統穩定辨認「學生剛完成什麼、現在第一個缺口是什麼」，再微調 Level 0／1／2 是否嚴格逐字遵守 prompt。

## 二、已正常運作、暫時不要重修的部分

### 1. Opener 的 stuck 規則正常

第一輪學生即使明確表示沒有思緒，`stuck_count` 仍維持 0；第二次及第三次卡住才依序進入 Level 1、Level 2。這部分目前符合需求。

### 2. Level 0／1／2 已有基本深度差別

最新測試的第一組提示已呈現基本層次：

- Level 0：指出需要有三個零點的輔助函數，讓學生思考二次項。
- Level 1：把問題縮小到利用三個已知函數值選擇二次函數。
- Level 2：直接給出具體二次式，讓學生執行計算。

嚴格來說，部分提示可能不完全符合目前 Level prompt；但現階段應先求三個 Level 有實際深度差異且引導品質穩定。單純 Level 不夠嚴格暫時不應成為硬性退件理由。

### 3. 最終 readiness 轉換正常

學生最後親自從 `g''(c)=0` 與 `g''(x)=f''(x)+8` 推出 `f''(c)=-8` 後，系統正確切換到 review，並主動要求學生提交完整證明。

### 4. Reviewer 的格式／服務穩定性正常

本次共有 17 次初審及 8 次重審，全部成功解析，沒有 `null`、timeout 或 JSON 解析失敗。因此目前不需要再調整 JSON schema、8192 token 或 timeout；現存問題是語意判斷品質，不是呼叫可用性。

## 三、P0：最優先修正的嚴重問題

## P0-1　學生進度判定仍混入 Tutor 訊息

### 測試現象

學生完整寫出三次 Rolle 推導，得到存在 `c` 使 `g''(c)=0`，Router 卻判為：

- `intent=uncertain`
- `has_actionable_math=false`
- `advances_solution=null`
- `turn_action=normal_guide`

Tutor 因而沒有承認這段正確推導，反而要求學生再說一次最後確定的主張。

### 根因

[tutor_driver.py](./math-proof-week2-main/dataset/tutor_driver.py) 的 `_has_new_math()` 及 `_student_state_context()` 在判斷新數學內容或重複內容時，使用「題目＋整段 conversation」。其中包含 Tutor 先前提示過的算式與結論。

因此，只要 Tutor 曾經提示某一步，學生之後親自推導該步，就可能被誤認為不是新進度。這也會污染 `proof_progress_revision`、stuck 重置、turn action 與 readiness 前置閘。

### 通用且簡單的修法

- `has_new_math`、`repeats_prior_math`、學生所有權只比較「題目＋先前學生訊息」。
- 排除所有 assistant／Tutor 訊息。
- `last_tutor_question` 仍可獨立傳入，只用於判斷學生是否回答上一問，不能用來否定學生所有權。
- 不改用題型關鍵詞，也不新增 proof graph。

### 驗收條件

- Tutor 曾提示某個結果後，學生首次親自寫出推導，仍須判為 `has_actionable_math=true`。
- 長篇正確或錯誤但具體的推導都應進入 `respond_attempt`；正誤交由數學審查器判斷。
- 純複製自己上一輪內容仍可判為沒有新進度。

## P0-2　明確的局部釐清問題會被判成一般引導

### 測試現象

學生明確表示：「我不知道要怎麼從 `g''(c)=0` 推導到 `f''(c)=-8`」，系統卻判為 `intent=uncertain`、`turn_action=normal_guide`，最後反問學生目前知道哪個式子。

這不是泛泛地要求提示，而是已明確指出「已知主張、目標主張及中間缺口」的局部釐清。

### 根因

目前 `_asks_specific_math_question()` 仍高度依賴問號及固定句型。沒有問號、但語意上明確詢問兩個數學主張如何連接的訊息，可能不會進入 `answer_clarification`。之後 Thinking 分類器若回傳 `uncertain`，Controller 也沒有用 `active_clarification` 作第二層校正。

### 通用且簡單的修法

- 在語意分類規則加入通用判準：學生若指出一個已知數學主張及欲得到的主張，並詢問兩者如何連接，屬於 `active_clarification`。
- action mapping 同時接受 `intent=request_hint` 或 `learning_state=active_clarification`，統一進入 `answer_clarification`。
- 不擴增「Rolle、導數、積分」等詞表；判斷的是訊息的語意角色。

### 驗收條件

- 「我不知道如何由 A 得到 B」不論是否有問號，都進入 `answer_clarification`。
- 「下一步是什麼？」這類沒有指出局部缺口的泛求助，仍可保留為一般 hint request。
- 適用平均值定理、極限、積分不等式、級數及 Taylor 證明等不同題型。

## P0-3　審查器沒有硬性檢查回覆是否承接最新學生步驟

### 測試現象

- 學生已完整推出 `g''(c)=0`，Tutor 要求他再重述最後確定的主張。
- 學生提出明確局部缺口，Tutor 又要求他重複已知式子。
- 學生正確算出 `g''(x)=f''(x)+8`，Tutor 卻只回答「你想先從哪個方向試試看？」；這個回覆仍被 reviewer 判定為 `passed`。

上述回覆沒有數學錯誤，但沒有正確處理最新學生訊息，會抹除進度、造成循環，並讓學生不知道剛才的答案是否正確。

### 根因

目前通過條件主要由多個負向欄位組成，例如數學是否錯誤、是否洩漏、是否完成學生未完成步驟。缺少一個獨立的正向判準：「候選回覆是否承接最新學生步驟並指向第一個缺少連結」。因此，安全但空泛的問句很容易通過。

### 通用且簡單的修法

- 在同一個 guide reviewer 增加 `addresses_latest_student_step`。
- 先輸出 `latest_student_step_status = correct / incorrect / no_step` 及 `first_missing_step`。
- 通過規則：
  - 學生答對：簡短確認該步，再問第一個缺少連結。
  - 學生答錯：聚焦錯誤或缺少前提，讓學生修正。
  - 學生卡住／詢問：提示目前第一個缺口。
- `addresses_latest_student_step=false` 時才重生成一次。
- 不增加 reviewer 呼叫次數。

### 驗收條件

- 正確答案後不能只輸出「你想從哪個方向試試看」。
- 學生已完成一大段推導時，不能要求他重複已明確寫出的結論。
- Tutor 每輪最多處理第一個未完成連結，不跳步也不倒退。

## P0-4　數學錯誤與錯誤所有權歸因仍會通過 reviewer

### 測試現象

- Tutor 把題目目標錯說成 `f''=8`，正確應為 `f''=-8`，但 reviewer 判定 `mathematically_correct=true`。
- 學生尚未親自得到 `g'(c_1)=g'(c_2)=0`，Tutor 已把它當成已知並推往下一次 Rolle；reviewer 沒有判定完成未完成步驟。
- Tutor 說「你自己算過 `g''` 是什麼」，但學生當時尚未算過；重審仍判定通過。

這些問題比 Level prompt 違規嚴重，因為會教給學生錯誤目標，或讓 Tutor 取代學生完成證明。

### 根因

目前單一 `feedback` 同時承擔學生進度、候選數學、ownership、Level 與 readiness 理由。模型可能在 feedback 中承認學生尚未完成某一步，卻同時把 `completes_any_unfinished_step` 判為 false，形成欄位矛盾。

### 通用且簡單的修法

維持一個 reviewer，但把輸出責任明確分開：

- `candidate_math_error`：候選中有錯誤才填具體錯誤，否則空字串。
- `candidate_ownership_error`：Tutor 假定、宣稱或沿用學生尚未完成的步驟時，填具體主張。
- `addresses_latest_student_step`。
- `ready_for_writeup` 與 `first_missing_step`。
- `level_policy_pass` 保留，但暫時只記錄。

審查規則增加兩個通用要求：

1. Tutor 若重新陳述題目結論，必須與題目中的結論數學等價。
2. Tutor 若宣稱「學生已算出／已證明」某主張，該主張必須能在 student-only 歷史中找到語意等價的證據。

這是語意及數學等價檢查，不是比對特定常數、函數或定理關鍵詞。

### 驗收條件

- 任意題目中，候選回覆把正負號、常數、導數階數或不等號方向說錯時必須退件。
- Tutor 不能把自己前一輪提供的結果說成「你已經證明」。
- 學生真的寫過等價步驟時，Tutor 可以承認，不要求逐字相同。

## 四、P1：完成 P0 後再修的問題

## P1-1　Level policy、回覆品質與 readiness 理由互相污染

### 測試現象

本次有 8 輪進行第二次 guide review，其中兩輪最後成為 `unresolved`。多次 `level_policy_pass=false` 的 feedback 只寫「學生尚未完成某步」，而不是候選回覆真正違反哪條 Level 規則。

學生證明尚未完整屬於 readiness，不應自動等於 Tutor 的 Level 回覆不合格。這種混用會把原本可能可用的候選換成脫離情境的保底句。

### 通用且簡單的修法

- `ready_for_writeup / first_missing_step` 只評學生累積證明。
- `candidate_math / ownership / relevance / leak / level` 只評 Tutor 候選。
- 同一輪重生成後，只更新候選回覆欄位，不讓第二稿改寫本輪已判定的學生 readiness。
- 依目前產品優先順序，若候選只有 `level_policy_pass=false`，但數學正確、ownership 安全、承接最新訊息且未洩漏，暫時允許送出並留下診斷紀錄。

### 驗收條件

- 「學生還缺最後一步」只能影響 readiness，不能單獨使 `level_policy_pass=false`。
- 同一則學生訊息的 readiness 不會因 Tutor 候選改寫而改變。
- 單純 Level 不嚴格不再導致通用保底句覆蓋高品質提示。

## P1-2　Level 深度雖存在，但提示品質不一定單調提升

### 測試現象

第一組 L0→L1→L2 有明顯加深；但第二組中，L1 已指出兩個導數零點及下一次 Rolle，L2 卻只說「最後一步你自己就能寫出來」，反而比 L1 更模糊，且帶有錯誤所有權歸因。

### 通用且簡單的修法

完成 P0 後，再檢查同一缺口內的提示是否逐級增加可操作性：

- L0：一個聚焦問題。
- L1：把同一問題縮小成更具體的子問題。
- L2：指出一個明確的定理／原理／方向，再用具體問題把執行交給學生。

現階段不必嚴格禁止 L2 出現所有算式；先確保 L2 比 L1 更有幫助、數學正確且不替學生完成整步。嚴格 prompt compliance 可在對話品質穩定後另行優化。

### 驗收條件

- 同一缺口連續卡住時，L1 必須比 L0 更具體，L2 必須比 L1 更可操作。
- L2 不得退化成「你應該會」「最後一步自己寫」等沒有新幫助的句子。
- Level 升級不應跳到第一個缺口之後的更後步驟。

## P1-3　保底回覆脫離數學上下文

### 測試現象

兩稿都被判不合格時，目前輪替使用：

- 請學生說最後確定的主張。
- 請學生檢查上一個推論的前提與結論。
- 請學生說一個已知式子。

這些句子本身安全，但可能要求學生重複剛寫過的內容，也無法回應明確局部問題。

### 通用且簡單的修法

- 保底回覆至少使用 reviewer 已判出的 `latest_student_step_status` 與 `first_missing_step`。
- 正確步驟：先確認，再問第一個缺口。
- 錯誤步驟：請學生檢查該步的依據或前提。
- 卡住／局部問題：針對缺口給目前 Level 相稱的提示。
- 不需要建立題型模板；只使用 reviewer 的語意結果。

### 驗收條件

- 保底句不得要求學生重複已明確寫出的正確結果。
- 學生指出局部缺口後，保底句不得回到更早的步驟。
- 同一缺口不能連續出現意思相同的保底句。

## 五、P2：測試與可診斷性

## P2-1　需要加入跨題型回歸案例

不要只用目前這一道 Rolle 題驗證。至少準備下列語意情境，分散在不同微積分證明類型：

1. Tutor 曾提示某步，學生之後親自完整推導該步；必須取得學生所有權。
2. 學生寫出一段很長但正確的局部證明；不得判為 `uncertain` 或 `has_actionable_math=false`。
3. 學生寫出具體但錯誤的推導；應進入 `respond_attempt`，再由數學 reviewer 糾正。
4. 學生說「我不知道如何由 A 得到 B」且沒有問號；應進入 `answer_clarification`。
5. Tutor 候選含錯號、錯常數、錯導數階數或錯不等號；必須被數學正確性攔截。
6. Tutor 宣稱學生已完成其實只由 Tutor 說過的步驟；必須被 ownership 攔截。
7. 學生答對目前一步；Tutor 必須確認並推進到第一缺口，不能輸出空泛問句。
8. 連續卡住時 Level 0／1／2 深度可辨識，且提示品質不下降。
9. 學生尚未親自推出最終結論時不得要求完整證明；親自推出後應立即要求完整證明。

題型應涵蓋：平均值定理／Rolle、極限或 epsilon-delta、積分估計、Taylor、數列或級數證明。每題沿用相同語意測試，不新增題目專屬規則。

## P2-2　診斷資料應能分辨是哪一層失敗

每輪至少保留：

- Router 的 `latest_student_step_status`、action 與判定來源。
- 初稿／重寫稿各自的 math、ownership、relevance、leak、level 結果。
- student-only readiness 與 `first_missing_step`。
- 最終為何採用候選、重生成或保底句。

這不增加模型呼叫，只改善記錄。後續測試才能分清楚是 Router 誤判、生成品質差、reviewer 漏判，還是 Controller 使用結果錯誤。

## 六、建議的最小 reviewer／Controller 結構

為避免再次把系統做得過度複雜，可維持一個 guide reviewer，輸入明確分成：

- 題目與參考證明。
- 上一個 Tutor 問題（只供判斷是否回答）。
- 最新學生訊息。
- 先前學生訊息（所有權來源）。
- Tutor 候選回覆。
- 當前 Level 與 action。

建議輸出：

```json
{
  "latest_student_step_status": "correct | incorrect | no_step",
  "first_missing_step": "",
  "candidate_math_error": "",
  "candidate_ownership_error": "",
  "addresses_latest_student_step": true,
  "leaks_final_conclusion": false,
  "level_policy_pass": true,
  "ready_for_writeup": false
}
```

Controller 順序：

1. `ready_for_writeup=true`：直接進入 review 並要求完整證明。
2. math／ownership／leak／relevance 任一失敗：重寫一次。
3. 只有 Level 不嚴格：現階段記錄但允許送出。
4. 第二稿仍不安全：使用與 `latest_student_step_status`、`first_missing_step` 對齊的保底回覆。

這個結構不需要新增 reviewer、不需要 proof graph，也不需要微積分關鍵詞列表。

## 七、後續實作範圍提醒

實際修改時，應至少同步：

- `math-proof-week2-main/dataset/tutor_driver.py`
- `math-proof-week2-main/dataset/phase_router.py`
- 對應 unit／phase routing／review workflow 測試
- 根目錄 `train.ipynb`
- 根目錄 `test.ipynb`

Notebook 應同步相同程式內容，避免 `.py` 已修正但 Colab 執行的仍是舊版。

## 八、暫時不建議採用的作法

- 不為 Rolle、Taylor、epsilon-delta 等題型擴增專屬關鍵詞清單。
- 不為每道題建立 proof graph 或手寫完整解題狀態機。
- 不拆成多個額外 reviewer 呼叫。
- 不因單純 Level prompt 不夠嚴格而犧牲數學正確、承接學生且有幫助的回覆。
- 不再優先調整 JSON schema、token 或 timeout；本次沒有 reviewer `null` 問題。
